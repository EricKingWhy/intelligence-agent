# Phase Status — 实施进度追踪

> **单一事实源**：任何 agent 进入项目前先读本文件，判断"当前做到哪里"。
> 规格文件（`SPEC_ROOT/14_IMPLEMENTATION_ROADMAP.md` 等，`SPEC_ROOT` 见 `AGENTS.md` §1）保持冻结，不被进度修改。
> 每完成一个 Phase，更新本文件对应行的状态 + commit hash + Gate 证据。

更新规则：
- `✅ COMPLETED`：Phase 全部交付物落地 + Gate 达成 + 测试通过。
- `🔄 IN PROGRESS`：部分交付物已落地，剩余项明确。
- `🔄 PARTIAL`：跨 Phase 的能力部分提前落地（如 Phase 3 工具在 Phase 1 阶段已部分实现）。
- `⬜ NOT STARTED`：未涉及。

---

## 进度表

| Phase | 名称 | 状态 | 关键 commit | Gate 证据 / 备注 |
|---|---|---|---|---|
| **0** | Repo Foundation | ✅ COMPLETED | `21b421d` 起 | Python 3.11+ / uv / pytest-asyncio / ruff / pydantic-settings / JSONL Diagnostic Logger。`config.py` + `logging.py` + 测试 harness 就位。 |
| **1** | SessionEvent + Model + Minimal Agent Loop | ✅ COMPLETED | `f789aac`（AgentRuntime）<br>`8ecb202`（bind_tools）<br>`87c3a72`（event-sourced） | SessionEvent DTO + 10 种 event type + JsonlSessionStore（崩溃安全）+ derive_messages（配对+dangling 合成）+ Session 聚合根（start/resume/append/derive/begin_run/end_run）+ AgentRuntime event-sourced 改造 + Resume 集成测试。Gate 达成：进程重启后可从 JSONL 恢复完整对话历史。135 passed。 |
| **2** | Tool Runtime | ✅ COMPLETED | `8572fd8` → `00f3753` | Tool Contract / Registry / ToolResult / Validation-first ToolExecutor / 单 Retry Layer / 批次调度 + 严格 ID 配对。Gate 达成：INVALID_ARGUMENT 不重试、transient 可重试、配对 100%。Permission interface 薄，留待 Phase 7 Capability seam 深化。 |
| **3** | Docker Sandbox + Coding Tools | ✅ COMPLETED | `2bfa457` → `ed96f5a` | Sandbox ABC（含 list_files）+ LocalSubprocessSandbox + DockerSandbox（懒加载 + 确定性命名 + 跨进程恢复）+ 9 个 Coding Tools（read/write/bash/edit/grep/glob/apply_patch/git_status/git_diff）+ 批次调度验证 ✅。**后续独立 spec 全部落地**：Approval / REQUIRE_APPROVAL 机制（PermissionPolicy + ToolPermission + ToolExecutor 审批关卡 stage 2.5 + per-call scoping，默认安全拒绝 danger）；Session-scoped Sandbox 生命周期（WorkspaceRegistry 映射持久化 + Session.start/resume 自动绑定 sandbox + Docker 后端跨进程恢复）。Gate 达成：edit 多匹配明确失败、pytest exit_code=1 不被 retry、两个无冲突文件操作可并发、冲突写被串行化、路径越界统一 PERMISSION_DENIED、DANGER 工具在非 full-access policy 下被审批关卡拦截。252 passed。 |
| **4** | Storage + Operation Ledger + Recovery | ✅ COMPLETED | `5b85e36`（#27 Ledger）<br>`6c94398`（#28 Checkpoint）<br>`fbcf599`（#29 RecoveryCoordinator）<br>`596c53b`（#30 Reconcile）<br>`778c1da`（#32 Kill 集成测试） | 全部 6 个 Ticket 落地：durable Operation Ledger（SQLite + aiosqlite，Ledger-first 写入顺序）+ 稳定边界 Checkpoint 持久化（CheckpointPolicy seam，checkpoint 绝不进事件流）+ RecoveryCoordinator（07 §9 冻结 8 步唯一入口，决策与写入分离，BEGIN EXCLUSIVE sidecar 恢复锁）+ UNKNOWN 人工裁决（ReconcileHint / ReconcileVerdict / ReconcileCallback，RUNNING→UNKNOWN→NEED_RECONCILE 两步状态机强制，RETRY 只能来自用户裁决，operation/reconcile-required 落盘）+ 真实子进程 Kill 恢复集成测试。Gate 达成：duplicate confirmed side effect = 0、dangling tool call = 0、Workspace 按原映射恢复、5 个独立 Kill 场景全过、PENDING 默认 skip 不盲跑。345 passed, 8 skipped；全仓 ruff clean。 |
| **5** | Artifact + S3 + Context Compaction（ADR-0006） | ✅ COMPLETED | `fd7439a`（#45）<br>`c98c9a5`（#46）<br>`de6a894`（#47）<br>`80086a9`（#48）<br>`e19905d`（#49）<br>`c0a4b0f`（#50） | ArtifactStore / Fake / S3 / inspect_artifact、Overflow Handler、ContextBuilder、三层 Compaction、Runtime/Web 装配全部落地。真实七牛 5000 行 Bash 溢出、局部回读、历史重建 Gate 通过。全量 424 passed；真实七牛 2 passed；ruff clean。证据：docs/PHASE5_GATE.md。 |
| **6** | Memory Capability / Context Provider | ✅ COMPLETED | `21a9e2f`（#51）+ `93d2e18`（#52）+ `62db7de`（#53）+ `e2a3a92`（#54）+ `df2b3e8`（#55）+ `5c6d40f`（deps）+ `96d02bf`（#56 Web 接线 + Review 修复）+ Gap 收口 commit | IdentityContext + SQLite MemoryRecordStore + Milvus 向量适配 + Outbox Relay + LangMem Capability + Extractor 三层降级 + Writeback + Web 入口接线全部落地；USER/SESSION 隔离、事务 outbox。**真实 Zilliz + SiliconFlow 嵌入 Gate 3 passed（连续两轮）**：真实 CRUD 闭环、语义检索排序、多租户隔离、schema mismatch、清理全过。503 passed、8 skipped、8 deselected，ruff clean。证据：docs/PHASE6_GATE.md。 |
| **7** | Capability / Plugin Foundation + Skills | ✅ COMPLETED | `4548cc2`（ADR+术语）<br>`fe94fab`（#59 config+wiring）<br>`e9b29b3`（#60 discovery）<br>`f589868`（#61 skills 闭环）<br>`2a84c0e`（#62 demo+Gate）<br>`984f4c9`（review 修复） | CapabilityRegistry 命名注册 + CapabilityDescriptor（08 §5 字段 + degradation 三分类 + enabled）+ CapabilityError 四码词汇表 + CAPABILITIES env JSON（pydantic strict）+ wire_capabilities 显式装配表（factory 失败按档降级 / REQUIRED_CORE 上抛 / 未知 provider 显式失败）+ Memory 迁入 Registry + SKILL.md 发现（frontmatter name/description 必填 + 可选 when_to_use 注入目录行，用户批准扩展）+ SkillCatalogContextProvider 只注目录 + load_skill READ_ONLY 统一 Executor + TickerCapability demo 纯 config 注册。Gate 达成：demo capability 全链路 Agent Loop 零改动（runtime.py 零 diff + ScriptedModel 闭环）、Skill 全文不默认进 Context（模型请求边界断言）、Optional 故障降级、插件不绕过 Permission/Ledger。585 passed、8 skipped、8 deselected，ruff clean。证据：docs/PHASE7_GATE.md。 |
| **9** | Streaming Surfaces | ✅ COMPLETED | `c4261b6`（SSE+run_stream 核心）<br>`d4a7709`（review 修复）<br>`db8ea96`/`daa03ef`（前端 Gap 收口）<br>本轮（CLI Renderer 补建） | 精简版提前施工（ADR-0005，用户授权跳序）+ 逐项收口。交付：AgentEvent stream（run_stream 逐 chunk delta + 持久化事件镜像，test_run_stream）；CLI Renderer（StreamRenderer 事件流纯函数渲染：delta 直写/工具状态行/结果预览/时长徽章/用量页脚，渲染约定借鉴 pi-mono & oh-my-pi 均 MIT，ascii 符号路线规避 GBK 编码崩；会话持久化 + SystemExit(1) 失败码）；FastAPI SSE + REST POST + Session query API（list/events/recover）；disconnect cleanup（断连取消臂补 run/failed(reason=cancelled)，无 task 泄漏，test_stream_disconnect_recovery）。Gate：ModelDelta/Tool 事件实时 ✅；SSE 断连无 queue/task 泄漏 ✅。 |
| **8** | MCP Client Capability | ✅ COMPLETED | `5b8e765`（#63 config+fake）<br>`0600738`（#64/65 transports）<br>`943d4e3`（#66 adapter）<br>`508371f`（#67 wiring）<br>`723f32e`（#68 Gate） | ADR-0012（grill 逐项拍板）驱动：MCP Client only（官方 SDK 2.1.1 REUSE+ADAPT）+ tools 原语 only；stdio + Streamable HTTP 双 transport；capability 接入（配置解析 extra=forbid 响亮失败 / ${VAR} 秘密间接引用 / stdio 白名单 env / 单 server 隔离降级 / 全下线跳过）；MCPToolAdapter（mcp__{server}__{tool} 命名、readOnlyHint→READ_ONLY else DANGER + 配置覆写、inputSchema→动态 pydantic、50KB 输出预算、MCP isError→failure(retryable=False)）；生命周期（wiring 时连接、重连只恢复连接不隐式重执行、lifecycle 通道挂 AppState.shutdown）；预设四 server（GitHub/chrome-devtools/Sentry/Context7）opt-in 模板。Gate 达成：① remote MCP tool 经统一 ToolExecutor（Ledger 记录 + DANGER 审批拦截 + SessionEvent 镜像）② 无双重 retry（SDK retry 关闭、死亡调用零隐式重放、isError 单次尝试）。测试 fake server 全程 in-process（真实大厂 server 手动验收清单 docs/PHASE8_MANUAL_ACCEPTANCE.md）。全量 761 passed、8 skipped、8 deselected，ruff clean。 |

| **10** | Lightweight Web Session Inspector | ✅ COMPLETED (UI 重设计) | `6183d3f` → `38fc775` | UI 重设计七阶段全部交付（Phase 1-7，docs/UI_DESIGN_DECISIONS.md 冻结决策落地）：Phase 1 设计系统基座（CSS 变量 token + 液态玻璃 + Apple 缓动）；Phase 2 Application Shell（三栏单帧 + Run Pulse 信号 #1）；Phase 3 Trace Ladder（Chat Refinement + 执行链投影签名 #2）；Phase 4 Trace Density 四档（localStorage 持久化）；Phase 5 Run Inspector 五 tab + 事件级钻取（InspectorFocus 判别联合）；Phase 6 Session Model E 轮 + DSH 工具四态（running/success/failed/stopped）+ UnknownSurface 兜底；Phase 7 Accessibility（WCAG 对比度 + focus-visible 全覆盖）。纯函数 Vitest 73/73，tsc clean，生产构建通过。前端单一投影源 lib/projection.ts 不变量 #22 守护。**后端集成 Gap 见 docs/INTEGRATION_NOTES.md**：usage/model/cost/trace_id/checkpoint API + 首条用户消息 payload 待后端 Phase 9 扩展。 |
| **11** | Knowledge / RAG | ✅ COMPLETED | `fc4eca0`（ADR-0013+术语）<br>`99883c2`（T1 服务+fake）<br>`958e887`（T2 Milvus）<br>`5367d57`（T3/T4 工具+CLI）<br>`b4d4cd3`（T5 接线）<br>本轮（T6 真实 Gate） | ADR-0013 grill 逐项拍板：Agentic RAG（检索=工具，模型自主决策，无自动注入）；Knowledge 与 Memory 平行域、独立 knowledge_chunks collection（tenant partition）+ 域专用协议（不泛化 memory 接口）；source 级增量（hash 跳过/原子重建、registry 提交点最后写崩溃自愈）；citation `kb:<source>#<idx>` 可追溯；sufficient 阈值 0.6（真实分数分布验证：相关 0.66–0.73 / 噪声 ≤0.29 干净分离）；2MB/2000 防呆；删除不暴露给模型。真实 Zilliz Gate 三条全过（restart persistent search / 不足如实 false / citation 链路）。证据：docs/PHASE11_GATE.md。 |
| **12** | Web Search / Reliability | ✅ COMPLETED | `fd8ede5`（#76-#81 全套）<br>`7358897`（review 修复） | ADR-0014（grill 逐项拍板）驱动：同错熔断 RepeatedToolFailureGuard（指纹=工具名+规范化 args，软 3 注入纠正 → 硬 6 end_run(failed)，#69）；统一 RetrievalProvider 窄协议（KB 与 Web 平行策略类）+ TavilyWebSearchProvider（手写 httpx 零新依赖，#77/#78）；web_search 工具（citation `web:<url>` + 防注入声明 + Retrieval Fallback Policy agentic hint，#79）；Model Fallback（is_transient_model_error 共享分类 + FallbackPolicy seam + ModelFallbackCoordinator never-切回 + runtime per-run 编排 + model/fallback 白盒事件，#80）；websearch capability 接线（TAVILY_API_KEY 未配 = OPTIONAL_RUNTIME 降级缺席）。真实 Gate 三条全过（Tavily 联网 citation 实测 / 死端点→真实 fallback 切换 reason=APIConnectionError / 真实模型 8 并行同参失败→硬熔断 identical_tool_failure_loop）。证据：docs/PHASE12_GATE.md。后续加固：DSML 泄漏守卫 + 流式卡流看门狗（idle 60s/total 600s → ModelStallError transient）+ MODEL_MAX_CONCURRENCY 进程级并发闸门。 |
| **13** | Multi-Agent | ✅ COMPLETED | `79a0014`（ADR-0015+术语）<br>`c72ec4c`（#82 profiles+factory）<br>`2096743`（#89 并发闸门）<br>`32a1593`（#83 capability+delegate）<br>`e2d03ef`（#84 事件+lineage）<br>`021ee6e`（#85 result 字段）<br>`97b4c9b`（#86 溢出）<br>`756602a`（#87 预算）<br>`e7b8360`（#88 熔断+#92 seam）<br>`0fc5057`（#90 并行）<br>`0f1d01d`（#91 取消恢复）<br>本轮（#93 Gate+文档） | ADR-0015（grill 逐项拍板，组织原则=「一切皆可是插件」）：multiagent 为 CAPABILITIES opt-in capability（ADR-0010 模式，未配置=单代理零感知）；AgentProfile/AgentSpec 核心域对象 + AgentFactory（scope 收窄生成新 registry、越权拒绝防提升，决策 11）；supervisor = main profile + delegate 工具（零新编排组件，DelegationDecision 即工具参数，经统一 ToolExecutor，决策 4）；child = 独立 Session + depth=1（父流只有 delegation-started/finished 白盒事件，child 完整历史在 child JSONL，决策 6/7/8）；SubAgentResult 真实来源字段（citations/artifacts/changed_files/unresolved 绝不伪造，决策 12）+ summary 溢出 store-backed/truncate+pointer；预算三旋钮 max_delegations=8/max_active_children=4/child max_steps=10 + 超限明确回填（决策 13）；重复委派熔断复用 RepeatedToolFailureGuard（#88 零新机器）；阻塞并行委派（executor 既有 gather 管线，#90）；取消/恢复语义（父 cancel → child 收尾 + 事件对账，#91）；OrchestrationAdapter Protocol-only seam（LangGraph 未引入，import 边界契约测试，#92）。真实 Gate 八条全过（research/coding 路由、动态第四 Agent、child 不倾倒、mixed 双 child 协作、熔断/预算真实触发、capability 关闭单代理回归）。证据：docs/PHASE13_GATE.md。 |
| **S-UI** | Streaming UI 生产级改造（detached-run / reasoning / 工具输出流 / 重连 / 多模型） | ✅ COMPLETED | `1b04445`（T1+T2 词汇+合帧+reasoning）<br>`849a901`（T3 工具输出流+预持久化）<br>`6337b8c`（T4 RunManager+detached-run）<br>`6a3ba27`（T5+T6 cancel+after_seq）<br>`961d8ec`（T7 多模型） | ADR-0016（用户经前端 prompt 拍板 D-A/D-B/D-C/D-D，数值与细节后端落定；与 feat/phase14 并行开发，ADR 分账 0016/0017，session/event.py 双方只做加法）。交付：① detached-run（web RunManager 订阅制广播 + seq 幂等合并 + 有界队列丢旧自愈 + 孤儿 300s 回收 reason=orphaned + POST /cancel 显式取消——Phase 9 取消臂语义有意修订，断连不再杀 run）；② reasoning 事件族（ReasoningChatOpenAI 接出网关 reasoning_content、30ms 合帧落盘、块生命周期 + interrupted 保部分内容、零伪造）；③ text/delta 合帧 durable（model/delta 词汇退役不发射）；④ 工具输出真流式（sandbox on_output → 线程安全 sink → tool/output_delta，64KB/channel 上限，tool/call 预持久化对齐规格 02 §8.3）；⑤ after_seq 重连续传（先订阅后取游标，重放+接续无缝无重复；backlog>1000 发 stream/truncated 控制帧；终态/悬空语义分明）；⑥ 多模型（AGENT_MODELS catalog + GET /api/models + 会话级 model 参数，fallback 链不动 ADR-0014）。契约回传前端：docs/BACKEND_CONTRACT_STREAMING_UI.md（text/delta 切换 / cancel 端点 / 重连协议 / 迁移清单）。离线全量 **1093 passed**、9 skipped、20 deselected，ruff clean。 |
| **14** | Resume / Replay / Fork 完整化 | ✅ COMPLETED | `bcaed59`（ADR-0017+术语）<br>`1685ed3`（#107 schema）<br>`c8f45b2`（#108 fork 核心）<br>`36edd1b`（#109 copy-on-fork）<br>`5ded3b6`（#110 tail summary）<br>`2e12074`（#111 CLI fork）<br>`0f70b98`（#112 lineage 树）<br>`3f8673e`（#113 Web API）<br>`2f476d4`（#114 replay）<br>本轮（#115 Gate+文档） | ADR-0017（grill 两轮，上游调研 pi/Claude Code/LangGraph/oh-my-pi）：file-per-lineage（session 线性 append-only 宪法不动，树在 SessionMetaStore 索引层）；fork boundary = run 完整边界（UX 按第 N 条用户消息）；seed = 前缀逐字复制（重编 seq 留原 event_id，child 自包含）；`session/forked` provenance 事件（parent/fork_point/boundary/tail_summary?）；lineage 双层（事件=真相 + meta=索引，fork|delegation 统一 origin，存量惰性回填）；copy-on-fork（物理策略独立，artifact ref 复用）；tail summary（pi branch_summary/oh-my-pi rewind-report 精神，失败降级不变量 #21）；CLI `fork` / `replay`（冻结 tool result 契约）/ `sessions --tree`；Web 只读 lineage API（独立 router，躲 ADR-0016 重刀）。真实 Gate 5/5 单轮全过（fork 全链/摘要/隔离/树/回放零副作用），Gate 抓到并修复 zhipu preset 缺册真 bug。证据：docs/PHASE14_GATE.md。重新执行式 replay / Web 发起 fork / 树内导航 DEFER。 |
| **15** | Observability + Evaluation | ✅ COMPLETED | `5b8e765`（#117 sink 骨架）<br>`…`（#118-#125 全套）<br>`86adedc`（合入 main）<br>`c29a4f9`（smoke + Gate 收尾） | ADR-0018（grill 两轮逐项拍板）：LangfuseSink 旁路骨架（settings 门控 + 故障隔离 + 熔断器，key 空=零 import 完全缺席）、RunTracer（trace=run + generation=model call + 六条终态臂 trace_id 回填）、tool span（attempt 链）+ SubAgent 嵌套 trace + context-build span、flush 生命周期、reconcile verdict JSONL 诊断行、评测骨架（datasets 真相源 + P0 金标 cases + 真实模型 smoke + Langfuse seed/Experiment）、ScriptedModel 提升进 src、真云 Gate（jp 区）。D7 DEFER 批（`dbb48a5`，待集成）：environment/release 透传 + tool_calls 轮 output 标记。trace_url 契约（`2d7f87a`，已合入 main）：官方 SDK 可点击 URL 与 trace_id 并列对称下发。证据：docs/PHASE15_GATE.md。 |
| **16** | Final Full E2E | ✅ COMPLETED + 集成后验证闭合 | `1c09677`（ADR-0019+术语）<br>`93174f1`（#126 T1）<br>`bad588a`（#127 T2）<br>`1a38339`（#128 T3）<br>`8bbbbe3`（#129 T4）<br>`3b308bd`（#130 T5 测试）<br>`87080dd`（Gate 收尾文档）<br>`cefdbe1`（合入 main）<br>`7a2b8db`（集成后验证修复：DockerSandbox bug + 文档精度） | ADR-0019（grill 三轮收敛，12 项决策）：分段独立断言 + 薄编排层（D1）、双层模型（D2）、全 fake 进 CI（D3）、真子进程 kill + Ledger reconcile（D4）、Docker probe-gated integration（D5）、mutating tool = coding 副作用对账（D6）、单 gate 文件 + helpers（D7）、Gate 6 指标精确化（D8）、范围边界不重复测已覆盖能力（D9）、fixture 混合（D10）、性能只量不裁（D11）、trace + report 是 Gate（D12）。Tickets #126-#130 全部交付。**Gate 6/6 PASS**：duplicate confirmed=0（G1）、dangling=0（G2）、core recovery all-or-nothing（G3）、citation validity 格式+可回捞（G4）、permission violation 阻断+放行（G5）、Full E2E reproducible 薄编排确定性（G6）。**集成后验证闭合**：Gate **12/12 passed（含 Docker 容器恢复 PASS，daemon 在场）**；真实模型 smoke Langfuse trace 结构与 Gate 断言一致；DockerSandbox `read_text` NotFound→FileNotFoundError 真 bug 已修。最终基线：全量 **1222 passed / 1 skipped / ruff clean**。上游调研：pi-mono `describe.skipIf(!API_KEY)` + mock model 双层模式同构。详见 `docs/PHASE16_GATE.md`。 |

---

## 当前工作焦点

**当前焦点：Memory V2 的 `#297`–`#301` 均已集成并 CLOSED。#299 AC10 已由冻结语料 Recall@6=0.95 与真实 Milvus hybrid 检索证据满足，集成 tip `fbb8a98c`；`#302` / `#303` 已解锁，`#304` 等待后二票。#299 验证、review 与证据见 `docs/phase_status/2026-09.md` L1258；#301 的已知 Vitest 超时见 L1257。旧 Memory 数据未清理，未修改 `.env` 或凭证。

**Memory V2 当前状态（2026-09-25）**：`#297`–`#301` 已 CLOSED 并集成；`#299` AC10 Recall@6=0.95 且真实 Milvus hybrid 证据已归档，GitHub issue 已 CLOSED；`#302` / `#303` 已解除阻塞，`#304` 等待后二票。父规格 `#296` 与 PRD、执行票路径见 `docs/SDD_TICKET_TRACKER.md`。旧记忆数据仍保留；clean-slate 只在 `#303` 依赖完成并核验目标和保留项后执行。

**`#297`（MEM-V2-1）Typed Memory lifecycle：已集成到 `main`，GitHub issue CLOSED。实现、review 与测试历史保留在 `docs/SDD_TICKET_TRACKER.md`。

**`#298`（MEM-V2-2）Durable Formation and Adjudication：已集成到 `main`，GitHub issue CLOSED；ADR-0043 及历史 review / 验证边界见 tracker 与月度归档。

**F18-A（`#282` 会话内可改权限档·后端）已完成并并入 main（merge `56d561ba`，2026-09-22；覆盖闸门 exit 0、快进 push `origin/main`、三方交叉验证一致）**：权限档与 `auto_approve` 由「创建时定、不可变」改为**会话内可变**（ADR-0041 Status → **`Accepted`**）—— 事件唯一 `permission/changed`、派生「**最后一次 changed 胜**」、生效时机 = **下一轮 run**（per-run 快照）、**有 pending 审批时 409 禁改**、升 `danger-full-access` 由前端确认。**F18-B（`#283` 前端）已完成并并入 main（tip `e10bfda`，2026-09-22；四道门禁全绿 —— `tsc -b` 0 错 / `vitest run` **1063 passed** / `oxlint` **0 error** / `vite build` 0 错，e2e 真机 `control-row` **24 passed** + `u-project-task` **10 passed**；快进 push `f1557e8..e10bfda`）**：pill 解禁 + 升 `danger-full-access` 走浮层**内联确认行** + 删假提示 `PERMISSION_MODE_LOCKED_HINT` + 投影「最后一次胜」（只动 `session_permission_mode`）+ 改档成功后强制重载该会话事件。**2026-09-22 第三手补记（用户批准「补全套」）**：`#283` 事后补**两轴独立审查**（A 轴 Standards / B 轴 Correctness·Spec **均通过**，P0 = P1 = 0；合计 P2 = 5 / P3 = 7，均不阻断）+ **作者红证**（改造前树 **8 failed / 239 passed**）+ `StepDetail.window.test.tsx:132` 登记进 B-29 已知 flake 表（含一次独立确认）；台账审查行按 §8.5.1 由 **2319 压到 784 字符**、7 栏齐全。

**F18 两票收口；回主线的三票里 `#244`/`#256` 已由 B-38 关单、`#248` 的 R1/R2 已由 B-39 收口（集成 / 关单见 B-39 条与 `docs/phase_status/2026-09.md` L729）；B-37 的 #287–#290 已完成实现、独立 review、全量与真实 Gate、main 集成、origin 推送及逐票关单；三仓 `main` 在本次 closeout docs push 后对齐到同一 tip。**

**当前主线：架构整改 `#244` / `#247` / `#256` 已闭合、`#248` 的 R1/R2 已由 B-39 收口**（排除前端 #236、性能线 #267/#274/#281）。**2026-09-22 两批（B-33 `#284` + B-34 `#285`）把"环境红"与"#265 残余"一起收口**：**B-33** 给依赖 `.env` 主模型链的 18 条真实调用挂上**环境守卫**（三态探测 + 四态裁决：任一端点可用 ⇒ 放行；全部**已分类**不可用 ⇒ 响亮 `pytest.skip`；**任一未分类失败 ⇒ 放行（fail-closed）**；配置不全 ⇒ 放行交给既有 `skipif`），判据是"配置链里有没有任一端点能完成一次最小调用"——整仓 `-m integration` 由 **17 failed / 17 passed / 6 skipped** 变为 **`failed` 恒 0**：主树（真实 `.env`，守卫路径）**16 passed / 24 skipped**；`%TEMP%` 冻结树副本（**无 `.env`**）**12 passed / 28 skipped**，那一轮的 28 条 skip 不是守卫产生的（守卫按设计放行 `ConfigError`）而是用例自己的 `skipif`；**冻结树副本 + 合成假 key 的 `.env`**（零真实凭据）实测守卫路径 **12 passed / 28 skipped / 0 failed**，skip 理由带 `[live-model guard]` 前缀 + 逐端点归因 ⇒ 判据面**可在无凭据环境里被复现**（16 vs 12 的差来自 `.env` 有无可选键 `FALLBACK_MODEL_*` / `TAVILY_API_KEY` / Milvus），其中 18 = 17 红 + **1 条假绿**（Phase 14 Gate4 的 fork 树渲染用例：断言面只读 `sessions --tree` 输出，模型调用失败照样绿）；守卫与用例共用同一配置边界 `settings_env_sealed()`（否则两条路径读不同配置），`SSLCertVerificationError` 必须先于本侧 `ValueError` 家族判定（CPython 多继承坑）。**B-34** 把 B-32 登记的残余 R1（调用关键字集合回 #264 前口径，哨兵 `_Unset`）/ R2+R3（超限臂**收口即清口**，取消臂与异常臂两条出口同时收口；公开参数 `keep_handle` 随之删除）/ ⑦（异常臂用例进仓库）/ ⑧（端到端断言面补到句柄身份 + 关键字集合）逐条收口，⑤ 复核（`grep` 零命中），并**新逮到 R4**（`interrupt_streams()` 抛错时收口段整个不执行 ⇒ 该路径 0 次收集；既有形状、登记不阻断）。两批各走两轴独立审查 + 修后窄复验（**均判"通过"**），窄复验的非阻断 P3 由 `8213164` 处置（脱敏断言从空转改为有牙、外层预算断言、三处措辞 + 408 取舍）；**变异实测**：脱敏那条与预算那条各自"恰好一条转红"，臂层变异 **2 条转红**（此前 0 条）。门禁在 `%TEMP%` 副本的冻结树上跑（主工作树与 `HEAD` 不一致：并行会话推进了 `HEAD`、其工作树/index 停在 `3b4e93c` 承接基线 ⇒ 主树读数不代表本批冻结树）；**已集成**：`push origin main` 快进 `56d561b..bafbd12`（`origin/main` = `HEAD`，0/0），`#284` / `#285` 按 §14.12 关单，随后 **4 笔 docs-only**（`f838a92` 落点记账 / `f28fcc5` 台账白名单 / `4cd66ef` 读数环境归因更正 / `a080912` 台账白名单；`git diff bafbd12..a080912 -- src tests` 为空 ⇒ `src`/`tests` 零改动）继续推到 **`a080912`**（**其后仅本批 docs-only 记账笔**，`git log --oneline a080912..origin/main` 可枚举；覆盖闸门复跑 **exit 0**：区间 `09ca47a1..HEAD` / 391 提交 / 已审查 274 / 待判定 117），三方 clone（后端 / 集成区 / 前端）集成后均对齐 **`origin/main` 顶端**。**并行线（非本批）**：另一会话在同一 clone 干 **#282**（F18-A 会话内可改权限档·后端，前身 #274 线），本批全程**路径限定提交**（`git commit --only`），只碰自己的 14 个文件；台账落行前先恢复到 **HEAD 版**（含并行线的 #282 行，逐字节保留）再追加本批行。历史：B-32 = `#265` telemetry 切片（`_Telemetry` 单点 owner，已集成关单）；B-31 = Phase 16 既有红修复（审批回调契约）。`#248` 的两处待裁决残余已由用户 2026-09-22 逐项裁决（**R1 = 搬到 `session/`**，先例 Temporal worker/runtime 包、Jupyter Server 把会话生命周期放在服务端包内；**R2 = 领域自建端口**，先例 langgraph 域内声明 store 协议、langmem 以自有 Protocol 接存储）⇒ AC2「部分满足」→「满足」（见 B-39 条），`#244/#256` 的 AC 面已闭合（`#244` 的 AC5 由 **B-38** 补齐、`#256` 的 AC1–AC4 证据此前已齐）⇒ 两票已按 §14.12 关单；`5db3d43` 的三层 Scope 偏离**不因关单而被追认**，仍按 §9.1.1 待用户裁决。**B-35（2026-09-22，用户批准直接修）**：把 B-33/B-34 登记的四条残余收口——① 探测行回显 `base_url`（`_redact_base_url` **六层**：整段 authority → userinfo → 密钥形词表（含 JWT）→ header/赋值形的值 → query/fragment → `_redact_detail`）；② 半配置在夹具层变 ERROR（配置谓词与用例 `skipif` **逐字等价**，"声明了却建不起链" ⇒ 响亮 skip 点名缺哪个 env，另加文本级漂移闸钉用例侧原文）；R4（`interrupt_streams()` 抛错 ⇒ 收口段整个不执行 ⇒ `_TerminalStages` 逐段兜底，取消臂 / 异常臂**两条出口同时**接入）；替身句柄分叉（专用替身改 `NullSpan` 同型）。**四笔** `3bf721e` / `9faa0b2` / `34e46ae` / `c7acb8e`（含三轮 findings 处置：无点主机与 IPv6 的 userinfo、authority 与 header 两层、两臂钉子补齐、隔离输入），**25 处定向变异逐条打红自己的钉子、无效钉子 0**（分层脱敏互相遮蔽 ⇒ 每条窄判据要配"无 scheme"隔离输入，这条方法学修正是三轮变异逼出来的）；门禁在冻结树（tree `1821a7be…` = `c7acb8e` 的树）全量 **3044 passed / 2 skipped / 42 deselected / exit 0**、Phase 16 gate **12 passed**、`ruff` clean、`git diff --check` exit 0。**父票 `#247` 自身验收面已核（用户授权）**：AC1–AC5 逐条取证（golden 243 / 配对·悬空 11 / 收口与释放 38 / 变异 M5 5 红与 M6 1 红——M6 的钉子**在 web 侧** `tests/web/test_runmanager.py`，只在 agent 侧四文件里跑会读到绿），三张子票均已 CLOSED ⇒ **已按 §14.12 关单**（`gh issue view 247` 复核 = CLOSED；AC5 三个读数关单前又在最终树 `3e77c1d` 上整轮复采，逐一相同）。**集成与关单已完成**：台账笔 `dbe0d9a`（覆盖闸门 exit 0）⇒ `push origin main` 两段快进 `4503d37..dbe0d9a` + `dbe0d9a..b17975e` ⇒ 三个 clone 对齐 **`b17975e`**（`HEAD^{tree}` = `452a6fe…`，三处逐字符相同）⇒ `#247` CLOSED（`#248` 不随本票关闭）。**B-36（2026-09-22，用户授权执行 `#281`）**：把 `#281`（B8 流式 chunk 聚合去 O(n²)；票面行号 `:791-802` 已漂移成 `:1117-1127`）做完——聚合块由「逐项 `+` 折叠」改为**一次官方归并**（`AIMessageChunk.__add__` 的列表形态 → `add_ai_message_chunks`：**逐项折叠走的是同一个函数**，故语义一行未改，只是把 N−1 次调用变成 1 次）⇒ 累计内容复制量由 O(N·L) 变 O(L)（确定性读数：4× N 时 **15.99×/16.00× → 4.00×/4.00×**；块语料墙钟 15.08×；端到端 `run_stream` 16k 档 **314.9 → 102.2 ms**）。**四轮独立审查 / 8 个子代理**（发现两轴 → 修后重审两轴 → 窄复验两轴 → 窄复验两轴），findings 全部闭合（**P0 = 0**；最值钱的是 R2 的【P3】**§1b 不自证**——计数器改坏时结论会**静默翻转**却无断言报警 ⇒ 补两条闭式断言）；**红证**：结构钉子（整条流只准发 1 次 `add_ai_message_chunks`）在改造前读 `[1]*11 != [11]` ⇒ `1 failed / 25 passed`，M1–M4 定向变异 1/13/12/12 红、**无效钉子 0**。**门禁（冻结树 `2570a00` / tree `dffcb897…`，隔离 clone）**：全量 **3070 passed / 2 skipped / 42 deselected / 337.72s / exit 0**、`tests/agent/` **484 passed**、AC4 复用 `#247` 的 golden（243）+ 本票新用例 26 = **269 passed**、`ruff` clean。**三条偏离主动登记**：AC6 字面未满足（多一个量测脚本 `scripts/measure_chunk_aggregation.py`，为"数字可复跑"付的 Scope 代价）、AC5 行号漂移、AC3 的耗时在 `width=10` 下**不呈超线性**（超线性只在确定性复制量与 §1c/§4 上成立——未包装成票面期望的形状）。父票 `#267` 验收面核查见关单 comment；明细见 `docs/phase_status/2026-09.md` L669。**B-38（2026-09-22，用户授权执行「未执行 / 未执行完」部分）**：`#244` AC5 补齐 —— `Settings.bash_timeout_seconds`（`Field(default=60.0, gt=0, allow_inf_nan=False)`）+ `BashTool` 预算注入**两闸**（`Settings` 构造期 `ValidationError` / 工具构造期 `ValueError`）+ 装配层唯一消费点（`assembly` 的 `BUILTIN_LOCAL_TOOLS` 循环；不接线即红）+ ADR-0039 §5 附录；**红证 17 failed / 1 passed**、变异四条（红 2 / 7 / 1 / 6，审查者独立复现）、实机探针两臂（配置 25.0 ⇒ 12 秒命令跑完 `wall=12.1 s`；配置 5.0 ⇒ `TIMEOUT wall=5.4 s`）；冻结树与集成树全量各一次 **3088 passed / 2 skipped / 42 deselected / 0 failed**（328.67 s / 411.22 s）+ `ruff` clean + `git diff --check` clean；两轮独立审查均 PASS-WITH-NITS（无 P0/P1）；集成按 §14.6 合并为 **`fe46f24`**（唯一冲突台账按 §14.7 第 4 条**并集**解析 + 独立审计六项）；`#244` / `#256` 已按 §14.12 关单。明细见 tracker `## B-38` 段与 2026-09-22 归档条。

**B-37（2026-09-25 完成）**：`#286`–`#290` 均 CLOSED；#287–#290 的最终代码已集成并推送到 `origin/main`，全量 pytest、review coverage、Gate-0 与真实 Memory/Knowledge/Tavily/Langfuse/Qiniu/Phase 13–16 Gates 均通过。每张 issue comment 均附对应 commit、测试、review coverage 与残余；三仓 `main` 对齐在最终 closeout docs push 后完成。详细 AC 映射、Gate 证据、review ledger 与清理结果见 `docs/SDD_TICKET_TRACKER.md` B-37 与 `docs/phase_status/2026-09.md`。

**文档导航（2026-09-21，B-30）**：`docs/` 的入口收敛为 `docs/README.md`（当前必读 / 按任务查阅 / 历史归档）；一次性集成提示词与交接资料已移入 `docs/archive/`（旧路径映射见各目录 `README.md`）。

## 更新日志（索引）

> **本文件不再内联历史明细。** 查批次 / 集成 / 审查记录：按「按日定位」用 `Read` + `offset/limit` 只读那一段，
> 或先 `grep -n "关键词" docs/phase_status/2026-09.md` 定位。**不要整文件读。**
> ⚠ 不要用 `tail` / `sed` / `cat` 读这些中文大文件：Windows Git Bash 的 GBK 控制台会把 UTF-8 显示成乱码（实测踩过）。

**单条 bullet 上限 2000 字符**（防止本文件再长回 500 KB）：明细超出就只在这里留一行索引 + 正文进当月归档文件。

### 最近条目（最新在上）
- 2026-09-26（T7 / `#315` run 的**绝对 deadline** —— **到点不硬杀、稳定边界收口成非终态暂停 + 换新时刻同 run 续跑 + 未证副作用先对账**，B 链第七票，分支 `zcode/T315-run-deadline`，基点 `f13ed0d`，十六笔 `9496226` → `1444e2e`（含证据与记录笔），**代码冻结树 `cf1879d`**、**最终 Live Gate 证据绑定 sha `cf1879d3` / tree `99a8b465`**；**已集成**（PR #333 → merge `5ea64103`，服务端 `gate0` pass 26s；`#315` CLOSED；landed 树 `91eb76f` 复跑 6/6 PASS / 16.85s））：`run.deadline_at` 绝对时刻（RFC 3339 UTC）在**准入前**判，到点后的调用被拒、**账上不留行**、不烧配额；到点在稳定边界收口成 `run/paused`（`reason=deadline`、closeout 确定性、**零新 Provider 请求**）；MUTATING 超时留下「未证副作用」⇒ Ledger 升 `NEED_RECONCILE` + `blocked_by` 改写，恢复先被 409 挡死（`03 §5` 对账优先于恢复）；恢复要**点名一个新的未来时刻** + CAS、同 `run_id`。**六轮审查面**：发现两轴（Standards 4×P2；Correctness **P1×1** —— 对账闸门原先只在 `reason==deadline` 时跑，而恢复闸门是 session 级 ⇒「预算暂停 + 未证行」会落成暗示可安全续跑的暂停）⇒ 处置 `aa003b4`+`24a1acc` ⇒ 补审两轴（各 1 P2）⇒ 处置 `98d56d6` ⇒ **权威前端车道 `tsc -b` 抓回真缺陷** `71f19f8`（投影从没读 `limits.run.deadline_at` ⇒ Web 面板那一行在真机上是空的）⇒ 定向两轴审查（含 **P2**：CLI 上「时刻从不出现」，票面 Must Do 名存实亡）⇒ 处置 `c6d9cd6` ⇒ 修后重审 8×P3 收尾 `cf1879d`；变异共 21 个、失败集互不相同。**真实 Live Gate 3/3 PASS**（每次 23/23 断言、收尾 paused / paused / completed ⇒ D9 两种安全结局同现；`validate --require-pass` 24/24 条 0 FAIL、exit 0），**两次被拒运行**保留入库作原始依据。**重车道**：全量 pytest（冻结树）**1 failed / 4277 passed / 14 skipped / 50 deselected / 720.53s**（唯一红 = 已证的 `core.autocrlf` CRLF 既有项；另两条一次性红无失败签名 ⇒ **待用户裁决**）、前端 `tsc -b` rc0 / `vitest` **1157 passed** / `oxlint` 0 error / `vite build` rc0 / `playwright --workers=2` **458 passed / 2 failed**（`#201` spec 字面量陈旧，**已在 `f13ed0d` 基线 clone 同 red 复现** ⇒ 既有红、非本票回归，属前端面，不修只登记；e2e 因 5173 占用走替用端口 5183）。**门禁**：Gate-0 裸全量 **6/6 PASS**（tip `28995ce`，墙钟 41.05s，落盘 `docs/gate/28995ce96dcf4da87054f1bc605e1023e205a6c3.json`；另跑 `git diff --check f13ed0d..HEAD` exit 0），**先回后正合并 `main` 后**在集成候选 tip 上再跑一次 **6/6 PASS**（`72541ee` / tree `9a834ae55551`，22.66s，落盘 `docs/gate/72541eed2909c54c1be2e15571be9bde690786ca.json`）；覆盖闸门 exit 0（台账 331–334 行）。明细见 tracker `## T7（#315，B 链第七票）` 段与 `docs/phase_status/2026-09.md` L1658-L1881（+ 集成落账 L1885-L1915）。
- 2026-09-26（T6 / `#314` 工具调用账与按名字的绝对配额 —— **接纳点唯一计数 + 逻辑调用/尝试两套账**，B 链第六票，分支 `zcode/T314-tool-quotas`，基点 `718ab562`，十二笔 `12ade5c` → `1314904`（含集成落账前两笔记录笔），**代码冻结树 `baf763bc`**、**最终 Live Gate 证据绑定 sha `462c5bd3` / tree `ddaa252c`**；**已集成**（PR **#330** → merge **`5e1236e9`**，服务端 `gate0` **pass 35s**；`#314` CLOSED））：一次被**接纳**的规范化调用记一次 `tool_calls`、每次**真实尝试**（含 retry）记一次 `tool_attempts`，准入点**之前**被拒的调用**显式记 0**（可审计）；`budget.run.tool_call_limits` 按**已注册**工具名给正整数**绝对**上限（缺省 = 不限但照计数），未注册 / 非法值在任何工作之前 422；用尽**复用 T4 的 `run/paused`**（可恢复，不是终态）。**两轴审查**：Standards **FAIL**（P1 = ADR §5 证据指针指错文件并提前宣告证据；P2 = 配了 ceiling 却从未调用的工具两端读数不一致）/ Correctness **PASS-WITH-FINDINGS**（含**真缺陷**：配额拒绝与真实失败同路喂进同错熔断护栏 ⇒ 单条消息 ≥7 条同参数调用会收成不可恢复的 `run/failed`，与「耗尽 ⇒ 暂停」冲突）⇒ 处置 `b9f9bbd`（只放过 `BUDGET_EXHAUSTED`；三处读数改「表在不在」；配置校验移到 `take()` 之前），3 条新用例**修前全红**；**修后定向重审** PASS-WITH-FINDINGS（4×P3）⇒ `317cf2cb`，按 §8.3 第 4 条末段**不开第三轮**。**作者变异** 4 条 / 4 个**不同**失败集合（8 / 2 / 7 / 2 红）。**真实 Live Gate**（同 SHA / tree）：`long-task-past-legacy-turn-limit` v3 **3/3 PASS**，`steps=15/16/16`（越过旧上限 10）、逻辑调用 **15/17/16**（第 2 次 = 16 个决策对 17 条调用：一条消息带两条 `read`）、per-tool `bash=12/12/12`、`ceiling={bash:24}` == durable 快照、实现路径全在 `agent_harness.*`，`validate --require-pass` **24/24 条 0 FAIL**；**首版证据**（`317cf2cb`）被权威全仓 ruff 车道抓回的 **`RUF023`**（处置 `baf763bc`，一行换序）取代后保留作原始依据，场景与全量 pytest 均**重跑**；重跑首轮因工作树不干净被 validator **正确拒收**（未入库，如实登记）。**门禁**：Gate-0 裸全量 **6/6 PASS**（tip `9ec35dea` / tree `658b90b0ad04`，墙钟 24.96s，读数落盘
`docs/gate/9ec35deaa8338e4618c66a540623780fecef9387.json`），另补 `git diff --check 718ab562..HEAD` exit 0（bare 运行车道 ① 只查工作树）；覆盖闸门 exit 0（台账 180 行）。**集成**：本地 `main` 快进到 `1314904`（树逐字节相等）→ PR **#330** → 服务端 `gate0` **pass 35s** → merge **`5e1236e9`**；`#314` 已 CLOSED；集成落账与 §14.9 回补见归档 L1620-L1653。明细见 tracker `## T6（#314，B 链第六票）` 段与 `docs/phase_status/2026-09.md` L1511-L1618。

- 2026-09-26（B-47 覆盖闸门第四条判据「零新增内容」：树 == 某父的树 ⇒ 自动归属；分支 `gate/zero-content-attribution`，**已集成 `main`（PR #323 → merge `d669ec90`）/ 分支已删 / 无独立 issue 故无单可关**）：承接 B-46 残余①的结构性根治 —— `strict: true` + 禁 `rebase` ⇒ 分支合入前必已并入 `main` ⇒ GitHub 造出的 merge 与父提交二**逐文件相同** ⇒ `--cc` 空表 ⇒ 两条归属通道**皆** fail-closed（白名单那一支同样拒空表）⇒ **每个 PR 合入都在 `main` 顶端重造一个 ❌**（PR #321 `c8e04cb9` 与 PR #322 `da7dfef1` 各实测一次，相距**仅一天**）。新判据把「**树对象相等**」变成机械可验的归属理由：空表是"核对不了"⇒ 仍 fail-closed，树相等是**正向证明** ⇒ 放行，且要求 `--cc` 文件表与 tree 对象两条**互相独立**的通路给出同一结论；`resolve_trees` 另按**行的形状**判成败（`git cat-file --batch-check` 对**不存在的对象也退 0**，只把该行变成 `<input> missing`）。四笔提交 `da7dfef1..3bd597be`；测试 9 条 = 7 条判据层 + 2 条**合成仓库**端到端（正控"树 == 父 2 的树 ⇒ exit 0"**修前是 exit 1**；反控"空表但树 ≠ 任何父 ⇒ 仍须 exit 1"拦住「凡是空表就放行」的假实现），**变异实测** M1 拆接线只红正控、M2 判据偷懒红 4 条含反控、M3 只红解不出树那条、M15 形状校验此前**无任何用例**能抓（已补）、M4/M5/M7/M9 定级良性、M6 属**真放松**须另案。**两轮两轴独立审查 P0/P1 = 0**：第一轮 2×P2 + 2×P3；第二轮（修后重审）2×P2 + 1×P3 **全长在第一轮改写的产物上**（`FAIL_HELP` 仍在教人写 `[whitelist]`；"19 个取并集"实为"树 ≠ 某父"的**总数**、真残余只有 **3** 个）⇒ 全部处置，§8.3 第 4 条不开第三轮。**四分类实测**（用闸门自己的 `files_of`，`089524a~1..da7dfef1` = 713 commit / 21 merge）：树 == 某父 **2** / 空表**且**树 ≠ 任何父 **3**（真残余，仍需真实审查行）/ 全文档 **12**（docs-only 那支按路径覆盖）/ 含代码 **4**（需审查行）。`ruff check .` 全绿、`tests/tooling/` **154 passed**；覆盖闸门三个读数（落行前 `717/465/252`，唯一 ❌ = 本批 4 笔，exit 1；行已写未提交 `717/469/248`，exit 0；台账落点笔提交后 `718/469/249`，**0 条 ❌**，exit 0；其中 `已审查` / `待判定` = 被台账区间覆盖 / 不在任何区间内，后者**不等于“未归属”**）；Gate-0 裸全量 **6/6 PASS**（读数同批落盘）。⚠ 本批**刻意不动**：冻结的 `scripts/check_review_coverage.sh`（用户裁决，亦无需重编号任何行号引用）、白名单**代码一字不动**（131 条历史条目去留属**用户裁决项**）、与本批无关的历史引文（tracker `:4747`/`:4813`/`:4885`、归档 `:589`/`:803`/`:808` 等）是当时实测原文**不回改**。⚠ **审查过程事故**（见审计 §9.4.4）：Correctness 轴子代理的合成仓库探针误落**另一个 clone**，受跟踪代码与提交**零损失**、约 25 个未跟踪 `.tmp_*` 草稿**不可恢复**。残余：推广到那 3 个合并需一条**更弱**的规则（另案）。明细见归档 L1268。
- 2026-09-26（服务端 Gate-0 + `main` 分支保护落地（PR #321，merge `c8e04cb9`）+ 覆盖闸门「空 `--cc` 合并提交」的机械归属（台账 209 行）+ 陈旧文档全量订正）：`gate0` 已是 `main` 的必需状态检查（`strict` / `enforce_admins` 为真 / 禁 force-push 与删分支），直推被服务端拒（`GH006`）⇒ 集成只剩「推分支 → 开 PR → `gate0` 绿 → 合并 PR」，且这两步各自**仍需单独批准**。合入当天 `main` 顶端唯一 ❌ 即那个 merge（`be7cd592..c8e04cb9`）：它 `git show --cc --name-only` **为空**（分支合入前已并入 `main` ⇒ 合并结果与父提交二逐文件相同），而闸门对空表**两臂皆 fail-closed**（白名单同样拒空表）⇒ 只能由覆盖行归属；**每个 PR 合入都会重现**，根治（树 == 某父的树 ⇒ 自动归属）按裁决**另开 PR**。本批零代码面：台账 209 行使 464 → **465** / 待判定 245 → **244** / exit 0；另按实测订正 6 个文件的过期口径（`AGENTS.md` §13.4/§14.4/§14.6/§14.10、`CLAUDE.md` §3 第 7 条、协议 §8.8.6 与执行面、`verification.md` §0/§4、审计第 6 行、集成执行单横幅），并收口 3 处；全批落在 docs-only 面内。`.github/workflows/gate0.yml` 的头部注释同步**刻意推迟**到闸门修法 PR（避免本 PR 改动判定它自己的 workflow）。明细见归档 L1259。
- 2026-09-25（MEM-V2-5 `#301` 已集成、推送并 CLOSED，tip `74db7d7`）：Tombstone 授权读取 + 管理 UI；后端全量 3811 passed / 31 skipped / 49 deselected，管理 E2E 20 passed，lint/build、review coverage 与 Gate-0 6/6 通过。全量 Vitest 有 1 条未改动 `StepDetail.window` 已知超时，详见 L1257。
- 2026-09-25（MEM-V2-3 `#299` AC10 收口）：真实 Milvus hybrid 检索对冻结的 20 个项目级跨会话查询命中 19 个，Recall@6=0.95（门槛 0.85）；证据文件 `docs/evidence/memory-v2-recall-ac10-2026-09-25.json`，集成 tip `fbb8a98c`，issue CLOSED。`#302` / `#303` 已解锁。细节见归档 L1258。


- 2026-09-26（T5 / `#313` Provider 请求 / token / cost 计入 run 预算 —— **计数点与绝对 ceiling 的执行面**，B 链第五票，分支 `zcode/T313-model-request-budget`，基点 `c7a1a8f0`（含 T2/T3/T4 全部提交），实现 `667ea1a8`，代码冻结树 `847b218e` / tree `b295b582`（+ 一笔内容零变更的模式位修复 `2d3761c`）、重跑证据绑定 `75909d5` / tree `5a5dad01`，**已合入 `main`（PR #326 → merge `fd50af4b`，2026-09-26）、已关单**）：`model/request` = 每一次**实际** Provider 请求一格（primary / fallback / closeout；被拒 / 传输失败也计，不增 `agent_turns`）；`BudgetConsumed` 四维 **`None` 粘性**（任一格没自报 ⇒ 整维未知，绝不记 0）；准入 `_dimension_reached` 与 headroom `_dimension_headroom` **两个谓词不合并**；`ProviderAccounting` 是**声明式**能力 ⇒ 不可强制的 ceiling 在**首个 Provider 请求之前** 422（`reports_cost=False` ⇒ 显式 `max_cost_usd` 恒 422）；`resume_limits` 未点名的维度**沿用**暂停时的值。**审查**：R1（审 `667ea1a8`）P1×2 含**真缺陷**（在途请求取消时未 `aclose` 内层模型流 ⇒ 那一格从账上消失）⇒ 处置 `b17830cc`；R2（锚 `b17830cc`）Standards PASS / Correctness NEEDS-FIX（终态不清 `paused` ⇒ 同一份投影同时说"等恢复"与"已失败"）⇒ 处置 `02daddd2`，按 §8.3 第 4 条末段不开第三轮；**发现阶段补审**（审 `b3685c29`）两轴 PASS-WITH-FINDINGS、**P0/P1 = 0**，18 条变异 16 条被抓 ⇒ 处置 `66046b8`。**真实 Live Gate**（同一 SHA / tree）：smoke 3/3、long-task 3/3（steps=16/16/15）、pause_resume 3/3（steps=8×3；暂停载荷 `model_requests`=2 / `agent_turns`=1 = primary:1+closeout:1）；AC-9 受控失败证据面 26/26 断言 ×3（3 席请求 / 1 轮；token 未知不写 0）（判定被锁 FAIL、永不进 3/3）。**首跑作废**：smoke 三次全 PASS 而总判定 FAIL —— 运行窗口内我把 12 个未跟踪 `.tmp_*` 移出仓库，runner 的"工作树偏离"判据 **fail-closed 正确拒绝**。门禁（冻结树 `847b218e`）：pytest **1 failed / 4163 passed / 2 skipped / 50 deselected**（唯一红 = `tests/memory/test_memory_v2_recall_dataset.py` 按**工作树字节**比哈希 ⇒ Windows CRLF 检出必红，属可移植性缺陷、非本票回归）、vitest 非确定（见 tracker 残余 4）、build / oxlint **rc 0**、Gate-0 **6/6 PASS**（`docs/gate/69c9b99de10204363e906002872c60f1de30ab85.json`；本地绿被 CI 抓回一次 `EXE001`，见 tracker）、覆盖闸门 exit 0。**§14.9**：开工前已回补 `origin/main` 49 笔（`95b4699`；两份 docs 索引冲突按并集解析、经用户批准；归档条目数按合并后实测订正 **320**）。**待裁决（P2）**：`run/completed.data.usage_total` 同 run 恢复后只报**最后一次执行**的切片，而四维账报 run 累计 ⇒ 前端在终态覆盖自己的累计时会丢"暂停前那一半账"。明细见 tracker `## T5（#313，B 链第五票）` 段与 `docs/phase_status/2026-09.md` 的 T5 小节。

- 2026-09-25（T4 / `#312` 预算到顶落非终态 `run/paused`、同一 run 带 CAS 续跑 —— **累计回合预算的执行面**，B 链第四票，分支 `zcode/T312-run-pause-resume`（基点 `dfdded6` = T3 记录笔，含 T2/T3 全部提交），真实证据绑定 `089de04` / tree `1292cab0`、最终冻结树 `fc3ad15` / tree `d27a1aad`，**已合入 `main`（随 PR #326 → merge `fd50af4b`，2026-09-26）、已关单**）：交付 5 笔 `33a00ec` → `0fdd197` → `eb50a6c` → `089de04` → `fc3ad15` + 台账笔 `f25be96`（5 条审查行）+ 本记录笔。要点：`ceiling=N` 只接纳 **N−1** 个产出轮（第 N 轮留给收口容量 `RESERVED_CLOSEOUT_TURNS=1`，判定在 loop 顶部、任何工作之前 ⇒ "低预算必然暂停"是结构结果）；`consumed.agent_turns` **只数 `model/completed`**（收口那次是 `model_requests`，"在 ceiling 之内"由预留表达）；同 run 续跑**不造新 run_id**、不落 `user/message`；CAS 唯一判定入口 `validate_resume`，形状非法 422 / 状态对不上 409 且**零副作用**；`run/paused` 非终态但按"已干净收口"收口（interrupt 尾部 pause 不算未终结 / fork −1 +1 / runmanager 抑制接力投递 / 记忆资格不含暂停）。**两轴审查 R1**（审 `33a00ec`）P0=0/P1=0，两条 P2（计数点 / `defer_model_event` 掺预算前瞻）+ **真缺陷 AC-8**（同版本并发 resume 两条都 200：CAS 锁挂在**每请求构造**的 `SessionService` 上）⇒ 处置于 `0fdd197`（锁改挂 `RunManager.session_lock`，真并发用例 `[200,200]` → `[200,409]`）；**修后重审 R2**（`33a00ec..0fdd197`）两轴 PASS-WITH-FINDINGS、0×P0/P1，**P2 一条是两轴共识 = F6 修复零鉴别力**（两轴各自回退判据 ⇒ 1640 条全绿）⇒ 处置于 `eb50a6c`（真 Ledger + ceiling=2 的判别用例，红证恰好 1 条），**不再开第三轮**（`eb50a6c` / `089de04` 代码面无独立审查轮覆盖，已登记）。**真实 Live Gate**：`budget-pause-resume-same-run` 真模型 + 生产工具 **3/3 PASS**（102.0s / 93.4s / 82.5s，每次 17/17 断言、`steps=8`），`validate` **24 条 0 FAIL**；首跑暴露**证据完整性缺陷**（读终态在 `shutdown()` 之前 ⇒ 可选能力收尾追加的 `memory/degraded` 让 `event_count` 与轨迹差一行、validator 如实判 FAIL）⇒ 修复笔 `089de04`，首版证据保留入库作原始依据。门禁（冻结树 `fc3ad15` / tree `d27a1aad`）：pytest **4016 passed / 2 skipped / 48 deselected**（507.01s，exit 0）、Gate-0 **6/6 PASS**（14.4s ⇒ `docs/gate/f25be96729c89519c63c916cd2cdf71464b6c107.json`）、`vitest` **1098 passed**、`build` / `oxlint` rc 0、覆盖闸门 exit 0。**集成顺序**：本分支含 T2/T3 全部提交、T1 的 `ADR-0044` / 规格只在 T1 分支 ⇒ 落位 **T1 → T2 → T3 → T4**。明细见 tracker `## T4（#312，B 链第四票）` 段与 `docs/phase_status/2026-09.md` 当日条。
- 2026-09-25（T3 / `#308` local 轮次预算 `budget.local.max_agent_turns` 契约落地 —— **默认 local fuse 500 + alias 与越权在任何工作前判定**，B 链第三票，分支 `zcode/T308-local-turn-budget`（基点 `08a9bdd`，冻结树 `bf4ddb7` / tree `401988c7`），**已合入 `main`（随 PR #326 → merge `fd50af4b`，2026-09-26）、已关单**）：交付 4 笔 `b3d99f4` → `67ecad6` → `0164e1d` → `ce23f19` + 台账笔 `bf4ddb7`（4 条审查行）+ 本记录笔。要点：四来源优先级（Deployment > 档位声明 > 请求 override > `max_steps` alias）**下层只能收窄**，越权 / 冲突 / 非法值在解析点拒绝且**不建 workspace、不落 JSONL、不构造模型**；alias 规则**全局**（同传且相等 ⇒ 接受，不等 ⇒ 422；删除归 `#320`）；**判定与运行态解耦**（queued / steer 也判、生效值丢弃）；`steps >= max_agent_turns` 落 `run/failed(reason=max_steps_exceeded)` 是票面钦定的**过渡行为**（**不实现 `run/paused`、无第二套 loop**，单一判定结果留给 T4）。**两轴审查 R1**（审 `b3d99f4`）**P1 两轴共识** = queued/steer 分支跳过判定 ⇒ 同一份请求体因运行态换语义；**修后重审 R2**（`b3d99f4..67ecad6`）**0×P0/P1**、**2×P2 两轴共识**（判定在 `cancel_queue` 之后 ⇒ 被拒请求先毁掉用户旧排队项 / 档位轴复发）⇒ 处置于 `0164e1d`，红证两条（`Left contains one more item: 'queue/cancelled'`、`agent_profile: None != 'coding'`），**按 §8.3 第 4 条 + §8.8.5 末条不再开第三轮**（`0164e1d` 代码面无独立审查轮覆盖，已在 tracker 与台账行 `305` 登记）。**真实 Live Gate**：`long-task-past-legacy-turn-limit` 真模型 + 生产工具 **3/3 PASS**（`steps=16` > 旧上限 10、`chain-steps.txt=12`、轨迹里无 `max_steps_exceeded`、fuse=500 source=deployment、10 条断言全绿），`validate` **24 条 0 FAIL**；首版 FAIL 证据（`token=` 触发赋值形态扫描）保留入库作原始依据。门禁（冻结树 `bf4ddb7` / tree `401988c7`）：pytest **3935 passed / 2 skipped / 48 deselected**（502.41s，exit 0）、Gate-0 **6/6 PASS**（13.7s ⇒ `docs/gate/bf4ddb799b5c34f37435f868e904a2b6414131f8.json`）、`vitest` **1068 passed**、`tsc -b` rc 0、`vite build` rc 0、覆盖闸门 exit 0。**集成顺序约束**：本分支含 T2 全部提交、T1 的 `ADR-0044`/`02 §5.1` 只在 T1 分支 ⇒ 落位顺序 **T1 → T2 → T3**。明细见 tracker `## T3（#308，B 链第三票）` 段与 `docs/phase_status/2026-09.md` 当日条。
- 2026-09-25（T2 / `#307` 真实模型 + 生产工具 Live Gate 证据运行器 —— **runner / evidence validator / 场景注册面**，B 链第二票，分支 `zcode/T307-live-gate-evidence-runner`（固定点 `d0e2dcb`，冻结树 `76ced9a`），**已合入 `main`（随 PR #326 → merge `fd50af4b`，2026-09-26）、已关单**）：交付 4 笔 `f8bb91e` → `c1d7bd2` → `45505e6` → `b0f92ea` + 台账笔 `76ced9a` + 本记录笔，给出四态判定（**PASS 只有一条路径** = 3/3 真实尝试全过且无注入无替身缝；BLOCKED 不发那 3 次 run）、**两类替身缝的机械核实**（`capability` = 注入的探测函数 / `scenario` = 场景类定义文件必须真落在 `evaluation/live_gate/scenarios/` 下）、反篡改判据（PASS 尝试缺 `events_sha256` / `event_count` / `tool_calls` 任一即 FAIL；非 PASS 缺字段 ⇒ UNAVAILABLE「未记录，未比对」）与凭证·工作区边界（`SecretStr` 精确值 + 形状层扫描命中即**不落盘**并判 FAIL；宿主路径替 `<workspace>`；一次性工作区跑完核实销毁）。**两轴独立审查均 FAIL**：P1 三处"能产出假 PASS"的面（`attempts` 旋钮 / 「记了才比」/ 替身场景可自称内置）⇒ 处置笔 `45505e6` 全修并各补判别性用例（`tests/live_gate` 72 → **95 例**），按 §8.3 **无第二轮**，另登记一条不改（`docs/live_gate/**` 不加 `text eol=lf`，依据入 `docs/agents/verification.md` §⑮）。三层真实证据（三份都绑定 `45505e6` / tree `02002825`）：PASS 3/3（`validate` **24 条 0 FAIL**、`--require-pass` 退出 0）/ 注入 `FAIL`（19 条 0 FAIL，第 1/3 次照跑）/ 无凭证 `BLOCKED`（**零 run**，6 条 0 FAIL）。门禁（冻结树 `76ced9a` / tree `9045ace`）：全量 pytest **3874 passed / 2 skipped / 48 deselected**（507.40s，exit 0，`PYTHONPATH=` 纯净环境）、Gate-0 **6/6 PASS**（墙钟 22.2s ⇒ `docs/gate/76ced9aff66bd19a90088a786899ad26ebad3af2.json`）、覆盖闸门 exit 0、`ruff` clean。`#319` 的五类场景**直接复用**本票的 runner / validator / 注册面。明细见 tracker `## T2（#307，B 链第二票）` 段与 `docs/phase_status/2026-09.md` 当日条。

- 2026-09-25（T1 / `#306` 长任务执行边界契约冻结 —— **ADR-0044 + 正式规格六章 + 决策覆盖表**，B 链首票，分支 `zcode/T306-longrun-budget-contracts`（交付 tip `ec4f73e`；集成前回补后 `c3657dc6`），**已合入 `main`（PR #328 → merge `7989e185`，2026-09-26）、已关单**）：交付 9 笔 `c4a696e` → `daaf662` + 台账笔 `3ed88e7` + 读数笔 `ec4f73e`，把 #305 的 FIXED 决策逐条写进正式规格（`02` §5.1–§5.4 三控制 / 七 counter / 暂停恢复 / stuck 五模式 / Quiescence 六条；`03` §3.4 `run/paused`·`run/resumed` + §5 Run 状态六值与 Crash durability；`04` §9.1 工具配额与 deadline 接纳边界；`10` §5.1 委派树预算 owner；`11` §6.1 接口契约；`12` §9.1 强制五场景 Live Gate），ADR-0044 只留决策与理由（数值与阈值**单点在规格**，去重后不再复制）。**两轴独立审查**：发现阶段两轴均 FAIL（18 条，去重 15 项；最值钱的是去重引入的阈值错序 `3/3/4/6/4` 与 `run/resumed` 在正式规格零落点、名字权威指向装不下它的生成物）⇒ 逐条处置后 **R2 无 P0/P1**（Standards 闭合 4/6、Spec 11/12；R2 在修订面新引入两条已当场修复）。门禁（冻结树 `3ed88e7` / tree `c8525a1`）：全量 pytest **3779 passed / 2 skipped / 48 deselected**、Gate-0 **6/6 PASS**（`docs/gate/3ed88e7b147f34522a3c4f30781f6a2d0d41dd68.json`）、覆盖闸门 exit 0、`ruff` clean。**零** `src/tests/web` 改动。**CI 往返（第三例「同一份脚本 ≠ 同一个环境」）**：首跑车道 ① FAIL（61 行行尾硬换行双空格，落在两笔摄入副本；本地裸全量不给 `--since` 时车道 ① 只覆盖工作树、pre-push 范围又只从远端分支 tip 起算 ⇒ 本地照不到）⇒ 处置 = 行尾**归一化**（`0b7d47c`；判据与 `.gitattributes` 均未改，门禁一条不减；等价性两条判据可复跑），连带把「与 #305 正文逐行一致」这条**自加**保证订正为「内容层逐行一致」（`7bb6c8e`）。代码面按 §8.1 继承 T5 冻结树读数（本票 0 文件代码变更）。**原登「待裁决」项已闭合**：ADR-0033 / 跨仓契约的 `max_steps_exceeded` 前向失配由 `#312` 同步（`0fdd197`：ADR 追加条 + 跨仓契约删除线口径；运行时该终态已不可达）⇒ 无需裁决。明细见 tracker `## T1（#306，B 链首票）` 段与 `docs/phase_status/2026-09.md` L813-L897 及本文件「按日定位」09-26 行的集成落账小节。
- 2026-09-24（MEM-V2-2（`#298`）Durable Formation and Adjudication —— **T1–T8 实现完成 + 两轴独立审查 + ADR-0043**，分支 `T298-mem-v2-2-durable-formation-adjudication`，**已推送、未集成、未关单**）：交付 16 笔（`1d9fc28` → `aa48eac`），把「run 终结 ⇒ 资格入队 ⇒ 模型形成候选 ⇒ 裁决 ⇒ 单事务终态」做成 durable；T6 抓到**跨切片 P0**（R2 投影不给真 `event_id` × 契约要真 id ⇒ 生产路径上自动记忆**零写入**），按用户裁决改为**运行时别名 `ref`**；T7a/T7b 接上宿主与装配（闸门 = `wiring.memory is not None`；作业表与记录表**同库文件**以满足 AC6/AC7 同事务；runner 进程级单例）。**两轴独立审查**（范围 `32ef89b..a88a0a0`，13 笔 / 36 文件 / 10493 插入，固定点 `a88a0a0`）Standards **NEEDS-FIX**（P0=0 P1=2 P2=2 P3=5 P4=2）／Spec **PASS-WITH-FINDINGS**（P0=0 P1=1 P2=3 P3=2），**16 条 findings（S1–S11 + P1–P5）全数处置**，逐条表在 ADR-0043 §5.3。门禁：`ruff check .` clean；`tests/memory/v2` 两片 **588**；V1 记忆 **225**；`tests/agent` **484**；装配/capability/守卫/web **178**；前端 `tsc` **exit 0**（修掉自 T6 起红的 `TS2741`）；**覆盖闸门 exit 0**（`09ca47a..HEAD` / 612 / 405 / 207）。**登记缺口**：9 类敏感无运行期检测器（§D8）／派生索引生产无驱动 ⇒ 裁决期「相似记忆」结构性为空（§D11，归属 `#299`/`#303`）。明细见 `docs/phase_status/2026-09.md` L808；证据评论 [issuecomment-5812364609](https://github.com/EricKingWhy/intelligence-agent/issues/298#issuecomment-5812364609)。
- 2026-09-23（B-45，`#295` 台账描述字段 lint + docs-only 按路径自动归属 + 语义等价判据 —— 交付 4 笔 `0437950` → `fa0a175` → `1fdd7e8` → `31ef526`，**已推送 `8f9ea1c..31ef526`（ff，Gate-0 6/6 PASS） / 已关单 `#295`**）：把 B-43 报告里三条**工具面**缺陷落成可执行护栏。**① 缺陷 7**：闸门新增 5 条描述字段 lint（`unbalanced_backtick`/`empty_parens`/`control_chars`/`overlong`/`unbalanced_bold`，阈值 800 与协议 §8.5 同口径），**默认 warn、`--strict` 升 fail**，每条配正控+反控。**② 缺陷 5（方案 C）**：新增 `is_docs_only()` —— 提交的全部改动路径命中 `DOC_PATTERN` ⇒ **按路径自动归属**，归属证据从「作者声明」升级为「路径客观事实」（实测白名单手写 docs-only 声明 **2 → 0 笔**）。**③ 缺陷 1**：新增 `scripts/check_semantic_equiv.py`（`ast.parse` → 剥三级 docstring → `ast.unparse`，fail-closed）。**协议新增 §8.9 写盘纪律**：凡把中文/长文本写进受控文件一律脚本落盘再跑、禁 `python -c` 内联。**两轴独立审查判不可集成（2×P1 + 6×P2 + 2×P3）⇒ 全数处置于 `fa0a175`**：P1① `AGENTS.md` §16.2 被写成协议逐字副本且指针 `§14.12` 消失、P1② 剥 docstring 时连坐真 `pass` ⇒ 删一行真语句被判等价（**直接推翻本票自己的反控样本**）。测试 24 passed、ruff clean、覆盖闸门 exit 0、「没放松」4/4 PASS。**集成已完成**：`8f9ea1c..31ef526` fast-forward 推送，Gate-0 **6/6 PASS**（墙钟 15.5s），`#295` 关单评论 [issuecomment-5792688513](https://github.com/EricKingWhy/intelligence-agent/issues/295#issuecomment-5792688513)。明细见 `docs/phase_status/2026-09.md` L798。
- 2026-09-23（B-44，`#294` 协议口径修订 —— 交付 2 笔，**纯 docs**，**已推送（`607dee4..b1c9dac` ff）/ 已关单**）：把 B-43 报告 §3 的五条口径类缺陷逐条落成条文 —— ① §8.1 第 3 条**并列新增**「纯注释 / 纯 docstring 的 `src/**` diff ⇒ 语义等价」例外（判据 = `ast` 剥离 docstring 与注释后归一化逐字节相同；三个控制样本的**实现归 `#295`**）；② §8.3 第 4 条**分列**「红证」与「变异」并给定义，要求变异覆盖 ≥2 个检查点且失败集合不同；③ §8.6 定「全量默认走 `scripts/run_tests_clean.sh`」与「门的判定 = 环境项之外 0 失败」口径（`gate0.py` 改动归 `#295`）；④ §8.1 第 6 条**硬要求**副本操作前探测行尾 + 断言锚点命中数 `== 1`；⑤ §8.8.3 增失败回退路径**覆盖登记附表**（B-42 = 触发 / B-43 = 未触发）。**判据只增不减**：改动前 707 条非空行一条不缺；协议 735 → 803 行，纯 CRLF；`test_verification_map.py` 20 passed；`git diff --check` rc=0。
- 2026-09-23（Memory V2 规划）：父规格 `#296` + 8 张子票 `#297`–`#304` 已发布并建立原生 Sub-issue / Blocked-by；PRD、独立票面与成熟产品研究已入仓工作树。本轮只规划/开票，未施工、未删旧记忆。明细见 `docs/phase_status/2026-09.md` L797。
- 2026-09-23（B-43，`#286` 动态 SubAgent grantable/depth 权限边界 —— 交付 3 笔 + 台账 1 笔，**已推送（`6e281c7..e800ecb` ff）/ 已关单**）：封死「委派出去的子代理把 `delegate` 拿回来」这条**运行期**逃逸通道。**三处成因互补**才构成逃逸：`AgentFactory.create()` 省略 `grantable` 时默认全量、`DelegationToolProvider` 正好省略它、`AgentSpec.max_depth` 只在 `__post_init__` 有值域校验没有运行期消费者。**交付**：`83e87f2` 红证（新 `tests/multiagent/test_delegation_depth.py` 8 例 + 工厂契约反转）；`ec61c26` 实现（新 `multiagent/depth.py` 把剩余深度做成 runtime-owned 事实 + `create(grantable=…)` 必给 + `provider` 逐层消费 + `assembly` 传根配额 + 6 处调用点）；`e58c1339` AC5 反面口径（缺席 ≠ 越权）。**红证 4 failed / 518 passed**（行为红，非签名红）；**变异 M1/M2 各 3 failed**、逐字节还原后 8 passed；**全量 3124 / 3 failed** 且三条全为 `tests/evaluation/*` 的 safe-delete 配额假红（同树清空 `PYTHONPATH` ⇒ 3 passed）；focused 524 passed；`ruff` clean；覆盖闸门 exit 0（`538 / 373 / 165`，`❌ 0`）。**两轴独立审查均 PASS-WITH-FINDINGS**（P0=P1=P2=0 / P3=1，两轴各自指出 `provider.activate` 的 docstring 对 `max_depth=0` 的描述夸大 ⇒ 登记残余①，不重开冻结树）。明细见 `docs/phase_status/2026-09.md` L784。
- 2026-09-22（B-42，`#293` 门禁读数机器落盘 + 台账「一文件一条」—— 交付 5 笔（`536a76e..26c0e86`）+ 修复笔 `79ae833` + 落点 + 台账 + JSON，**已推送 / 已关单**）：把门禁读数从「**人抄**」变成「**机器落盘**」，并给台账建立可并行的分文件形态。**三件套**：① `scripts/gate0.py` **裸全量**运行落盘 `docs/gate/<head sha>.json`（`sha` + `^{tree}` + 每车道 `status/rc/seconds/cwd/argv` + 墙钟 + 工具版本 + `worktree` 证据 + `scope.does_not_cover`；**增量运行（`--affected`/`--since`/`--only`）不落盘**；落盘前三道独立守卫 —— 追踪文件偏离 / `assume-unchanged`·`skip-worktree` 位 / 未跟踪的车道输入）；② 台账审查行由单文件 `docs/review_ledger.tsv` 拆为 `docs/review_ledger.d/<seq>-<base>-<tip>.tsv` **117 个一文件一条**，覆盖闸门 `scripts/check_review_coverage.py` **双读**（旧单文件在前 + 目录按名排序在后）、`is_ledger_only` 放宽为「恰好只改一个台账文件」⇒ 记账笔自动放行；③ 判定逻辑**一行未改**。**读数来源改指**：`AGENTS.md` §14.10 与协议 §7 第 8 条改指 `docs/gate/<sha>.json`，**「人抄读数」这条路径本身删除**（缺文件 ⇒ 现跑一次 Gate-0 生成，或按 §8.7 第 3 条作废重跑——**不要凭记忆补数字**）。**同批并入**：§8.3「没被任何一轴读过的新内容 ⇒ 按发现阶段补审，一票一次」边界（裁决 A，兑现 `#291` 超预算 1 轮）+ `5db3d43` 三层 Scope 偏离的**形式化追认 + 重定 Scope**（裁决 B1，登记 tracker B-25 段）。**验收对账**：拆前/拆后**同树**三种组合（旧脚本+旧布局 / 新脚本+旧布局 / 新脚本+新布局）输出 **311 行、只 1 行不同**（`审查范围（台账，117 行）` → `… = 旧单文件 0 + 目录 117`），三元组三次全等、`❌` 集合三次皆空、退出码同为 **0**。**已闭合（四项裁决，2026-09-22 晚）**：R2 三条 P1（`--replay` 签名错 ⇒ 复核路径抛 `TypeError` / 非 ASCII 未跟踪路径被 `git status` 做 C-quote ⇒ 守卫漏判 / `.git/info/exclude` 藏台账文件 ⇒ 闸门由 exit 1 误放行为 exit 0）+ P2（`ruff` 实扫 `docs/gate/**`，与本侧注释相反）；三条 P1 均**已独立复现**，按用户裁决「修复」由 `79ae833` 处置、两轴修后重审 PASS-WITH-FINDINGS（0×P0/P1/P2）。明细见 `docs/phase_status/2026-09.md` L769。
- 2026-09-22（B-41，`#292` 增量验证：验证映射表 + 守卫 + `gate0 --affected` —— 代码/测试 6 笔 + 文档 5 笔 + 落点，**覆盖闸门 ❌ 11 → 0、fast-forward 推送 `784df9e..c9ef006`（13 笔）、按 §14.12 关单**）：把「这次该跑什么」从**记忆**变成**可被脚本消费的表**，并给协议 §8.8.3 里 Review / RV 两行那个「必须由机械映射给出受影响集合」的空位填上实现。**三件套**：`docs/agents/verification.map.tsv`（**25 行 × 8 列**（B-41 落点时为 24；2026-09-25 补 `.github/` 服务端门禁行），feature 四要素由**五列**承载 —— `Driving it with <harness>` 拆成 `lanes` + `focused`；另加机械列 `surface`（只允许路径前缀 / 精确路径，禁通配、禁 catch-all）/ `layer` / `neg_tier`（`blast-radius` 阶梯））+ `tests/test_verification_map.py`（**20 例**守卫，①–⑦：每个被跟踪文件都被映射 / 结构合法 / **每行承重** / `focused` 真跑得动 / **每行列无条件车道** / user POV 形状可判 / 独立实现逐项相同；跑在 Gate-0 的 `guards` 车道 ⇒ **映射腐烂 = 推送前就红**）+ `scripts/gate0.py --affected <rev>`（按改动面收敛车道，**默认行为不变**）。**五条边界**（协议 §8.8.9 与 `verification.md` §4 同源）：只用于**失败后的增量重跑**；未映射路径 ⇒ fail-closed；**改动面为空 ⇒ 同样 fail-closed（不收敛成 0 条）**；同时给 `--since` ⇒ 一律以 `--affected` 为准；`focused` 只内联 pytest 子集。**实测（最终树 `HEAD=65f8d1f0a455` / tree `3cbc12dd63f2`，同轮同树，口径 = 工具自报耗时）**：三态 `--affected` = **A 88.9 s**（含内联 `tests/session` 82.21 s，点名唯一要补跑的重车道 `pytest-full`）/ **B 12.7 s**（点名 `vitest` + `build`）/ **C 3.7 s**；同树全量 Gate-0 **17.5 s**、全量 `pytest tests` **560.29 s**（`3103 passed, 13 skipped, 42 deselected`，junit `tests=3116 / failures=0 / errors=0`）、全量 `vitest run src` **28.33 s**（67 文件 1067 例）⇒ 「整条重来」≈ **606.1 s** vs A **88.9 s** = **约 6.8×**。三元组对账（map 侧独立实现 / `affected_summary` 侧 / CLI 侧）三态**全 `true`**、`unmapped` 全 `[]`。**空面 fail-closed 实测**：`--affected HEAD` ⇒ 打印「改动面为空 ⇒ fail-closed：跑全部车道」并**实跑 6/6**（`Gate-0 FAIL：5/6 通过，墙钟 16.8 s`）；修复前打印 `Gate-0 PASS：0/0 通过`（假绿）。**变异证明**（隔离克隆，正控 20 例全绿，主工作树零改动）：删 `web/src/` 整行 ⇒ 覆盖面红／加 `docs-dup` 重复行 ⇒ 承重红／`focused_runner` 复原成血证①写法 ⇒ 内联策略锁红／`neg_tier` 改 9 ⇒ 值域红／**摘掉某行的 `coverage` ⇒ `parse_map` 解析期当场 `ValueError`**（本批新补的牙）；克隆逐字节还原后复跑 20 passed。**两轮独立审查**：R1 3×P1（读数指着还没有 map 的树 / guards 例数写错 / **5 行漏列无条件车道 ⇒ 静默少跑**）+ 若干 P2，处置于 `a5ee459`/`09c908f`；R2 修后重审（读范围 `a5ee459..783c871`）Standards **NEEDS-FIX**（1×P1＋4×P2＋4×P3）/ Spec **PASS-WITH-FINDINGS**（2×P2＋1×P3），findings 经 `65f8d1f` 全数真修，**该处置笔按 §8.3「无第二轮」未再开审查轮**。**另两处口径更正**：F12 倍数口径（旧写法把两轮的数混着除 ⇒ 改为同轮同树、两半都用工具自报耗时）、日期口径（本批提交时间戳已过本机午夜，按与 B-40 一致的既有口径**一律记为 2026-09-22**，如实登记）。**索引按归档实测重算**：落点前实测 `^- 2026-` = 304、09-22 = 9、区间 L628-753，而本文件旧写 303 / 8 / L628-736 ⇒ B-40 落点未随归档更新该行，本批一并按实测重算；**同轮还逮到第三处**：既有 B-33/B-34 条目把 B-34 写成 `L645`（**实为 `L650`**）⇒ 一并更正。明细见 `docs/phase_status/2026-09.md` 的 B-41 条。
- 2026-09-22（B-40，`#291` 增量验证：七阶段主干 + 失败回退边界表定稿 —— 交付 4 笔 + 落点 2 笔 + 声明 1 笔，**已推送 + 已关单**）：把需求方 2026-09-22 定稿的主干（`Grill → Spec → Tickets → Implement(TDD/Tests/Matt 双轴) → Runtime Verification → Evidence Gate → Merge`）与「失败时**不整条流水线从头跑**」的**边界**写进协议，而不是留一句原则。**新增协议 §8.8**（8 个小节）：§8.8.1 七阶段主干（阶段名的唯一权威，§9 改为只钉"到哪个阶段读哪个文件"）；§8.8.2 **INV-1**（读数复用的唯一合法前提 = 能指到树 + 代码面未变，复用 §8.1 第 3 条两条判据）；§8.8.3 **失败回退边界表**（9 档，逐档写"回退到哪 / 必须重跑 / 环内可跳过 / 跳过为何不放松"，**最远只回退到 Spec**）；§8.8.4 永不因增量而跳过的 7 项（第 1 项 = 集成前完整门禁，第 7 项 = Runtime Verification 这个阶段本身）；§8.8.5「针对新 diff 的 review」定义并与 §8.3「无第二轮」逐条对齐（仍两轴 / 1 轮每轴 / 无单轴通道 / 台账写真范围）；§8.8.6 与 §8.7 **逐条对账（8 条一条不落）**；§8.8.7 复跑要求 + 首轮实测读数；§8.8.8 各档的方法依据（兑现票面 comment 的 2 条 AC：9 档逐一给 `docs/agents/skills/**` 路径或明文"不需要引"）。另收紧 `docs/agents/verification.md` §4「机制上没有必要」的适用范围到 6 条机械车道 + 写死 `--affected` 的边界。**实测（隔离副本：变异只在主工作树之外的副本里做；副本 tip `e378273`、tree `b59464332b06…` 与主仓同值）**：机械车道级 `gate0.py --only ruff` **2.4 s** vs 全 6 车道 **21.1 s**（**8.8×**）；测试级 `pytest tests/context/test_tokens.py` **4.6 s** vs 后端全量 **509.2 s**（**110.7×**，0 failed）—— 两个失败都是真实观察到的（ruff F401；`encode_ordinary→encode` 触发 tiktoken `ValueError`），非估算。**审查 4 轮**：初始两轴（Standards NEEDS-FIX 1×P1+6×P2 / Spec PASS-WITH-FINDINGS 5×P2，两轴 0×P0）→ 修后重审两轴（均 PASS-WITH-FINDINGS、新 diff 0×P0/P1）→ 兑 AC 笔的窄审两轴（Spec NEEDS-FIX 1×P1）→ 末轮处置笔不再开轮。**两个坑**：票面验收有 2 条写在 **comment** 里（第一轮只读 issue body ⇒ 漏做）；两轴对"§14.10 有没有点名 e2e/真机"给出**相反结论**，读原文后按 Standards 轴更正。**⚠ 待裁决**：第 3 轮超出 §8.3「每轴 1 轮」预算（理由：`4622c12` 新增的 §8.8.8 从未被任何一轴读过），末笔 `1d72485` 未再开审查轮。明细见 `docs/phase_status/2026-09.md` 的 B-40 条。**集成**：`push origin main` ⇒ `e378273..446db82`（fast-forward，7 笔；推送前 `.githooks/pre-push` 自动跑 Gate-0 **6/6 PASS / 墙钟 18.1s**，改动面 6 文件全为 docs）；覆盖闸门 `09ca47a..HEAD` **498 / 已审查 353 / 待判定 145 / ❌ 0 ⇒ exit 0**；`origin/main` = `446db82ac06d3c65885455944e0e4e593b59c955`（`git ls-remote --heads origin` 实测，本地 5 refs + 4 tags 逐条零损伤）；`#291` 已按 §14.12 关单（comment 载 7 笔实现提交 + 三项验收读数 + 4 轮审查 + 超预算披露 + 残余 5 条）。
- 2026-09-22（SDD 提速批 1/批 2 —— 审查机制修订 + 机械闸门可运行化；**13 笔待推**）：诊断结论是「**药是改机制，不是压字符**」（实测一票墙钟 **83%** 花在处置审查意见：B-35 = 137/165 分、B-36 = 35/53 分，且两票 docs 新增 0 行）。**批 1**（`5c7db24` + 台账笔 `22aa291`，已推送）：协议 §8.3 删「例外（不算超预算）」—— 该例外正是"修后重审"被无上限展开的入口（实测把 B-35 拉到**五轮**、B-36 拉到**四轮**，末轮只改注释与措辞）⇒ 改「**无第二轮**」；新增第 7 条「**工具已经管的事，审查不再报**」（= 向 Matt `/code-review` 原版 `Skip anything tooling already enforces` 靠拢，本仓此前未搬这句），附前提条款（前端 `oxlint`/`tsc -b`/`vitest` 齐备 ✓、后端当时**无 ruff 配置** ✗、两侧**无 CI / 无 hook** ✗ ⇒ 未具备项两轴仍须照报）。**批 2**（交付 4 笔 `66cc1fa`/`9527559`/`22992e4`/`acde040`，更正 2 笔 `ebb28b4`/`9bb51c4`，台账笔 `aa5f9e2a`）：① `scripts/check_review_coverage.py` —— 覆盖闸门**可运行实现**（`.sh` 冻结为语义参考；两版在**同一棵树**上三元组 `457/317/140` 逐项相同、退出码同为 1、逐条判定 **140/140 全等**、`❌` 集合相同）；② `scripts/gate0.py` 推送前 6 车道门禁（**预算目标** ≤60s，超了只告警、不改判定）；③ `.githooks/pre-push`（纯 POSIX 内建、找不到解释器 **fail-closed**）+ `.gitattributes` 钉 hook 行尾；④ `docs/agents/verification.md` 车道编目（①–⑪）。**审查**：四轮独立只读子代理（含**批 1 补审**），无 P0/P1；**同批自曝并修掉 3 个施工者自己写出的真 bug**。**闸门**：`09ca47a..HEAD` 464 提交 / 已审查 331 / 待判定 133 / `❌` **0**，**工作树与干净检出两个口径均 exit 0**。**未推送**（13 笔，等批准）。工作记录与待答问题见 `docs/agents/SDD_ACCELERATION_AUDIT.md`。
- 2026-09-22（B-39，`#248` R1/R2 用户裁决落地 —— 交付 3 笔 + 并入 main）：`#248`（领域服务改显式 collaborators）留下的两处**待裁决**残余按用户 2026-09-22 逐项裁决闭合。**裁决与依据**：**R1**（`RunManager` 归哪一层）选「**搬到 `session/`**」——先例 = Temporal 把 worker/runtime 放在服务端包内、Jupyter Server 把会话生命周期放在服务端包内而传输层只做适配；**R2**（恢复子系统怎么注入）选「**领域自建端口**」——先例 = langgraph 在域内声明 store 协议、langmem 以自有 Protocol 接存储（而非依赖具体实现类）。**交付面 3 笔**：`8310e62` = R1 `git mv web/runmanager.py → session/runmanager.py`（R099 重命名；logger 更名 `agent_harness.session.runmanager`）+ R2 域内 `@runtime_checkable RecoveryStoreBundle` 端口（4 成员 `operation_ledger` / `checkpoint_store` / `session_meta_store` / `workspace_index`）+ 10 处 import / 注释指针 + 守卫改扫 `session/` 三份域文件；`a54c13a` 审查 findings（组合层 import 守卫收全 `from X import Y` / `from X import assembly` / 相对导入 / 多别名 `import` 四种等价写法：只看 `node.module` 会整类绕过）；`848586e` 复验 N 条（守卫精度与覆盖面补齐 + 三处指针订正 + 一处 ADR 事实更正）。**红证 7 条**（`.workbuddy/red_248_r1r2.py`：域文件里插组合层白名单外 import、`web/runmanager.py` 复活探针、端口成员缺失等，逐条打红并断言改动后文件 hash 复原）；「home 钉在 `session/` 包」那条**在改造前不可达**（当时模块就在 `web/`）⇒ 如实登记为取证缺口，未包装成红证。**门禁三轮全量**：**3090 passed / 1 failed（本文件已登记的既有 flake）→ 3092 passed / 0 failed → 3093 passed / 2 skipped / 42 deselected / 0 failed in 487.24 s**（树 `848586e`），集合数逐轮对账可解释（3093 = 3092 + 1）。**三轮独立只读审查**：R1「可集成」（2×P2 + 4×P3）→ R2「修复成立」（N1–N7）→ R3「N1–N7 全闭合，无 P0/P1/P2」⇒ 收口。**残余登记不阻断**：属性链绕过（AST 守卫只覆盖 import 面）、端口只给**弱保证**（无 mypy 类型检查、`isinstance` 只查成员存在、多一个成员不会被判出）、动态 import 属声明范围外。明细见 `docs/phase_status/2026-09.md` L729。**集成与关单**：按 §14.6 合并为 `2036ad9`（并入 origin/main `e1a09d4`；两处冲突按 §14.7 并集解析，独立只读子代理九项审计判「可集成」）→ 覆盖闸门 exit 0（444 提交 / 已审查 317 / 待判定 127）→ `push origin main` `e1a09d4..056970f`；三个 clone 对齐 `056970f`（tree `a95fd2e9…` 三方相同）；`#248` 已按 §14.12 关单。**收尾**：实记笔 `e489dc9` + 台账笔 `2d6c549`（docs only）推送后三方对齐 `2d6c549`（tree `56055448…`），此后本批无代码面变更（`git diff 2d6c549..origin/main -- src tests web scripts` 为空可验）。
- 2026-09-22（B-38，`#244` AC5 Bash 预算配置入口 —— 交付 2 笔 + 台账 1 笔 + 并入 main）：把父票**最后一块 AC 面**补齐——此前 Bash 预算唯一落点是 `tools/bash.py` 的模块常量，**源码里不存在「配一个坏值」的位置** ⇒ AC5 无从判定。**交付**：`054ee3f` `Settings.bash_timeout_seconds`（`Field(default=60.0, gt=0, allow_inf_nan=False)`）+ `BashTool(sandbox, *, timeout_seconds=…)` 构造期 `ValueError` 守卫 + 契约钉子；`3c556e0` 装配层接线（`assembly` 的 `BUILTIN_LOCAL_TOOLS` 循环 = **唯一消费点**；不接线即红）+ 装配钉子 + ADR-0039 §5 附录；`6ce1e52` 台账 2 条审查行。**红证 17 failed / 1 passed**（改动前冻结树 + 仅两个测试文件；3 × `AttributeError` / 7 × `DID NOT RAISE` / 6 × `TypeError` / 1 × `assert 60.0 == 25.0`，唯一 pass 是有意的默认值钉子）；**变异四条**（`config.py` 默认 60→45 ⇒ 红 2、去掉 `gt=0`/`allow_inf_nan` ⇒ 红 7、装配不接线 ⇒ 红 1、去掉工具层守卫 ⇒ 红 6，均由审查者在隔离副本独立复现）；**实机探针两臂**（真 `ToolExecutor` + 真实 `LocalSandbox`：配置 25.0 ⇒ `ok=True wall=12.1 s` 跑完 12 秒命令；配置 5.0 ⇒ `ok=False / TIMEOUT / wall=5.4 s`）。**门禁**：冻结树（tree `cae29108…`）全量 **3088 passed / 2 skipped / 42 deselected / exit 0**（328.67 s）+ focused `tests/sandbox tests/tooling tests/tools` **378 passed / 1 skipped** + `ruff check .` clean + `git diff --check` clean；集成树（`fe46f24` = tree `af1e0287…`）同数读数 **in 411.22 s / exit 0**，且集成树 `src`/`tests` 子树与冻结树**对象相等**（`3ad9a7b6…` / `16d25289…`）⇒ 读数传递成立；既存 Docker flake 一次按 §8.6 同树 3 次单跑处置（fail/pass/pass），最终树 0 failed。**两轮独立审查**（R1 全量轴审 `054ee3f` / R2 delta 轴审 `3c556e0`）均 **PASS-WITH-NITS**，findings 与 nit 全数处置、无 P0/P1。**集成**：`origin/main` 已在 `165ae9f`（并行线 F18-A/F18-B + `#286`–`#290` 开票批）⇒ 按 §14.6「先回后正」合并为 **`fe46f24`**（parents `6ce1e52` + `165ae9f`），唯一冲突 `docs/review_ledger.tsv`（两侧皆**追加**）按 §14.7 第 4 条以**并集**解析（184 + 186 − 交集 181 = 189 行、0 冲突标记、双向包含自证无丢行），其余 20 文件自动合并 + 独立审计六项入台账；「冲突解决后的 `git add`」按 §14.4 本应单独获批，本次按用户对本批的集成 / 推送常设授权一并执行并留痕，**供用户事后否决**。`#244` / `#256` 已按 §14.12 关单（comment 明写：关单**不追认** `5db3d43` 三层 Scope 偏离 —— 该裁决仍待用户；父票跨模块 Gate = 本批实机探针 + `#258` Docker parity 证据 + 全量门禁，B-38 未另跑 Docker 实机臂）。**残余三条**（`demo/live_agent.py` 自建 registry / `sandbox/local.py:32` 不同源退回值 / 程序内 `timeout_seconds=True` 被当 1.0 秒）登记不阻断。明细见 `docs/phase_status/2026-09.md` L712。
- 2026-09-22（F18-B，`#283` 会话内可编辑权限档·前端 —— 交付 1 笔 + 台账 1 笔 + 并入 main）：F18-A（`#282` 后端）的配套前端。**投影**：`permission/changed` 由 F18-A 登记的 **no-op** 改为真投影（**last-wins**；非法值归 `null` 且**不回退** `session/started`；只动 `session_permission_mode`、不碰 `permission_policy` = `#236` 边界）⇒ 解除 F18-A 残余①。**交互**：会话内 pill 解禁；升级 `danger-full-access` 走**浮层内联确认行**（`role="alertdialog"`，点「取消」**不发请求**）；**删除** `PERMISSION_MODE_LOCKED_HINT`（改档可行后它是假话 —— ADR-0041 决策⑧ 的消解方式）；有 pending 审批 / 本轮进行中 ⇒ 禁用但文案**中性**；改档成功后**强制重载该会话事件**（重读→重投影，不做乐观本地态）。**交付面 2 笔**：`463b598` `feat(web):`（`web/src/**` + `web/e2e/**` 16 文件 +641/−86）→ `e10bfda` `chore(review-ledger):`（审查行 range `f1557e814a0d..463b59839ec7`）。**门禁（最终树）**：`tsc -b` 0 错；`vitest run` 65 文件 **1063 passed / 0 failed**（连跑两轮同读数）；`oxlint` **0 error**（42 警告皆既存 React-Compiler lint）；`vite build` 0 错（2116 模块）；e2e 真机 Chromium `control-row.spec.ts` **24 passed** + `u-project-task.spec.ts` **10 passed**。**独立归因（失败集合差集）**：`StepDetail.window.test.tsx:132` 曾红一次（`Test timed out in 5000ms`，单跑 4386ms ≈ 预算 88%）⇒ A/B 纯净树 **1052 passed / 0 failed** vs 本树 **1063 passed / 0 failed** ⇒ **差集 ∅ = 非回归**（差 11 = 新增用例）；该临界用例登记为残余（不在本票范围）。**集成**：首推被拒（远端被并发批次推到 `f1557e8`，6 笔纯文档）⇒ 两笔以 plumbing 重放到 `f1557e8`，重放后 `web` 子树 `rev-parse` 逐字节相同；**快进 push** `f1557e8..e10bfda`，`origin/main` = `HEAD`（0/0）。**残余**：① 临界超时（解除 = 另开票补 `testTimeout`）；② 会话内改档 e2e 仅 `control-row.spec.ts` 两例（解除 = 出现创建面回归时补例）；③ `COMPOSER_AUTO_APPROVE` 模块常量（解除 = 需要 UI 单独切 `auto_approve` 时扩展）。**第三手补记（2026-09-22，用户批准「补全套」）**：事后补两轴独立审查（A 轴 Standards / B 轴 Correctness·Spec **均通过**、P0 = P1 = 0，合计 P2 = 5 / P3 = 7 均不阻断）+ **作者红证 8 failed / 239 passed**（改造前树）+ `StepDetail.window.test.tsx:132` 登记进 B-29 已知 flake 表（含一次独立确认）；台账行按 §8.5.1 由 **2319 压到 784 字符**、7 栏齐全。明细见 `docs/phase_status/2026-09.md` L698。

- 2026-09-22（F18-A，`#282` 会话内可改权限档·后端 —— 交付 5 笔 + 台账 1 笔 + 并入 main）：权限档（`read-only`/`workspace-write`/`danger-full-access`）与 `auto_approve` 从「创建时定、不可变」改为**会话内可变**。**ADR-0041**（Status `Proposed` → **`Accepted`**）：事件唯一 `permission/changed`（data 同含档位 + `auto_approve`）、派生改「**最后一次 changed 胜**」、生效时机 = **下一轮 run**（per-run 快照，结构性免费）、**有 pending 审批时禁止改档（409）**、升 `danger-full-access` 由**前端**确认、三概念分离（`declared_at_creation` ∥ `effective_now` ∥ `policy_at_approval`，不与 `permission_policy` 合并 = `#236` 边界）。**交付**：`0d2e0de` 入 durable 词汇表（+两生成物 +三守卫，`test_event_store.py:88` 期望集**手改**）/ `754c23f` 派生 + 唯一写入口 `append_permission_change`（折入 `approval.py`）/ `3fa3c48` 端点 `POST /api/sessions/{id}/permission` + pending 409 + fork 继承 effective 档 / `c5993a5` 用例 917 行 / `22fb990` ADR-0041 + 本地执行索引（含 AC2 红证）。**10 条 AC 全过**（AC7 `test_permission_mode_persistence.py` **一字未改** = `git diff` 空；AC2 红证 = 改造前树派生仍 `READ_ONLY` vs 工作树 `DANGER_FULL_ACCESS`；AC8 **双向**证明不改 `permission_policy`）。**两轴独立审查**（Standards + Spec，固定点 `3b4e93c6`，审未提交工作树）：findings 全数处置，**无 P0/P1 代码缺陷**。**集成**：`origin/main` 已在 `7e872014` ⇒ 按 §14.6 先回后正合并为 **`56d561ba`**（唯一冲突 `docs/review_ledger.tsv` 的纯 append 碰撞 ⇒ **并集**解析，集合运算证明零改写零丢失；其余 12 文件三方自动合并干净）；合并审计 0 冲突标记 / 0 路径丢失；**快进 push** `origin/main` `7e87201..56d561b`。**残余**：前端投影 no-op（解除 = `#283` 合入）、seq-retry 两处重复（解除 = 第三处出现时抽公共）、非法档位走 422 而非 400。明细见 `docs/phase_status/2026-09.md` L627。
- 2026-09-22（B-36，`#281` B8 流式 chunk 聚合去 O(n²) + 父票 `#267` 验收面核查）：用户授权执行（截图六票里未执行 / 未执行完的部分）。票面 `## Blocked by` 是 `#247` ⇒ 开工前实测 `#247` **已 CLOSED** ⇒ 阻塞**自然解除**（不动用票面那条「例外窗口」）。**方案 = 票面的 C**：`AIMessageChunk.__add__` 的**列表形态**直接转发 `add_ai_message_chunks`——**逐项折叠走的是同一个函数**（每步只喂 1 个 other），故合并语义**一行未改**，改的只是"N−1 次调用 → 1 次调用"：累计内容从"每步重抄一遍"变成"只复制一次"。**交付面 3 文件 +700/−2**：`6bee833`（实现 + 新 golden 用例 26 条 / 398 行）→ `7a382c4`（R1 findings：复制量口径分列 + 等价性注释标注版本前提）→ `94c6f73`（R2 findings：计数器闭式自证 + `list` 口径 + §1c 第二把尺子）→ **`2570a00`**（R3 finding，**docstring-only**：口径注释里"裸 tuple 交给 `merge_content` 会抛 TypeError"实测不成立 ⇒ 改成真正的判别条件"当前累计值是 str 还是序列"）。**红证**：结构钉子（整条流只准发 1 次 `add_ai_message_chunks`）在改造前的 `runtime.py` 上读 `[1, 1, …, 1]`（11 个 1）≠ 期望 `[11]` ⇒ `1 failed / 25 passed`（**红行正是这一条**；其余 25 条是"两臂等价"型，改造前后都该绿）；M1 改回逐项折叠 1 红、M2 反序 13 红、M3 丢尾 12 红、M4 只归并首个 12 红、对照 26 绿；两轴各自独立复现（轴 A 复现 M1、轴 B 自写反序 / 丢尾各 12 红——**改动点不同故红数不同，两个读数都留档**）。**审查（四轮 / 8 个独立只读子代理，主工作树零改动）**：R1 发现两轴（无 P0；【P1】AC7 数字未落 + 【P2】红证读数无落点 = docs-only 面，由本次落点闭合；【P2】计数器只认 str ⇒ 块语料 0/0 假象；【P3】等价性缺版本前提；【P3】`(list, tuple)` 分支误导；【P4】带 `index` 的元素走 `merge_lists` 是"合并"非"追加"）→ R2 修后重审两轴（**【P3】§1b 不自证** = 最值钱的一条：计数器改坏 ⇒ 结论**静默翻转**成"naive 4.00x"而无断言报警 ⇒ 加两条闭式断言；【P4】§1c 缺第二把尺子；【P4】`WIDTH` 耦合）→ R3 窄复验两轴（轴 A **PASS**：独立复现"改坏 ⇒ 响亮失败"、§1c 15.17×；轴 B **PASS-with-findings**：3 条变异全响亮失败 + 【P3】口径注释不实）→ R4 窄复验两轴（**两轴均 PASS**：逐叶 AST diff 证"可执行代码零改动"、6 项断言逐条复现属实、9 组组合未得反例；7 类 content 形态实测"只认 `list`"**不反转任何结论**；两轴各 1 条 P4 非缺陷 ⇒ 登记不修）。**门禁（冻结树 `2570a00` / tree `dffcb897fb75f9492aae67676f4cd58e88dc8691`；脚本 blob `9daf8171…`；读数在隔离 clone 取并先断言 `agent_harness.__file__` 落在 clone 内）**：全量 **3070 passed / 2 skipped / 42 deselected / 9 warnings in 337.72s / `PYTEST_EXIT=0`**、`pytest tests/agent/` **484 passed / 3 deselected**、AC4 的 golden + 新用例 **269 passed**、`ruff check .` **All checks passed**。**冻结读数不用 §8.1.3 的传递**（门禁就跑在待集成的那棵树上），且主工作树上有并行会话（`#282`）**17 条已暂存未提交**改动 ⇒ 判据 ② 在本 clone **不可用**，替代证据更强：**index 指纹窗口前后逐字符相同**（`git write-tree` = `cb946f815cc2b605cb021fffdb820ac5c26b887d` 两次同值）⇒ 对方暂存路径窗口内没动过。**偏离三条主动登记**：① AC6 字面未满足（`git diff --stat` 是 3 个文件，多出量测脚本 `scripts/measure_chunk_aggregation.py`——票面要求前后对照数字却没给量测工具，写成一次性临时脚本则数字**不可复跑**，违反 `PERF_BASELINE.md` §1 第 3 条；代价：`scripts/**` 不算 docs-only ⇒ **必须在新树上重跑全量**，本批照做 3 次）；② AC5 行号漂移（`#247` 同文件施工导致）；③ AC3 的耗时在 `width=10` 下**不呈超线性**（4.27× / 4.68×，被 per-chunk 常数淹没）——超线性在确定性复制量（15.99× / 16.00× → 4.00×）与 §1c（15.08×）/§4（13.45×）上成立，**未包装**。**AC4 按要求显式声明**（复用 #247 的 `test_event_sequence_golden.py` = 243 + 本票 26 = 269 passed）。明细见 `docs/phase_status/2026-09.md` L669。
- 2026-09-22（B-35，B-33/B-34 残余四条收口 + 父票 `#247` 验收面核查）：用户批准直接修的四条残余（**不另开票**）：**①探测行回显 `base_url`** ⇒ `_redact_base_url` **六层**（整段 authority → userinfo → 本模块密钥形词表（含 JWT）→ header/赋值形的值 → query/fragment → `_redact_detail` 兜底），`EndpointProbe.line()` 的 `base_url` 与 `detail` 同走；“**无点主机名 / IPv6 字面量**（`localhost:8000` / `ollama:11434` / `[::1]`）的 userinfo”是二轮 B 轴实测逼出来的加固（产品 `validate_base_url` 只校验 scheme+netloc ⇒ 本地 / 内网自建端点是现实写法）；**②半配置在夹具层变 ERROR** ⇒ 配置谓词与用例 `skipif` 收到**逐字等价**（不 strip、不看 provider），"声明了却建不起整条链" ⇒ 响亮 skip 点名缺哪个 env + 诚实尾句，另加**文本级漂移闸**钉用例侧原文；**R4**（B-34 新逮到的既有缺口）⇒ `_TerminalStages` 逐段兜底（段抛错记结构化日志并继续，**取消臂 / 异常臂两条出口同时接入**，第一处异常在全部段跑完后原样重抛）；**替身句柄分叉** ⇒ `test_tracer_port.py` 专用替身改 `_IdentifiedNullSpan(NullSpan)` 同型。**五笔 + 后置处置**：`3bf721e`（+8 用例）→ `9faa0b2`（一轴 findings 处置）→ `34e46ae`（二轮 findings 处置：密钥形词表并入 URL/detail 面、句柄分叉 docstring 如实化、+4 用例）→ `c7acb8e`（三轮窄复验处置：整段 authority 与 header/赋值两层、JWT 分支、两臂钉子补齐（异常臂第二段 / 段故障日志 / 句柄身份）、隔离输入、+5 用例）→ `eadeb20`（四轮证伪复验处置：`?` 隔离行去掉 scheme 使其**真隔离**、注释归属与计数口径如实）→ `3e77c1d`（五轮闭合复核的后置处置，**comment-only**：换掉两个不成立的注释例子、如实写自定义 scheme 边界，**regex 一字未动**）。**红证**：半配置 A-B 对照（合成假 key 的 `.env` 副本：修前 `ConfigError` 夹具层 ERROR ⇒ 修后点名 env 的 skip）；**25 处定向变异逐条打红自己的钉子**（含"改写用例侧原文"这条钉在**别处文件**上的漂移闸），**无效钉子 0**——其中 6 条首轮"全绿"暴露出**分层遮蔽**（新补的宽层先于旧窄层命中 ⇒ 旧层收窄删掉也不变红），据此给每条窄判据补了**无 scheme 的隔离行**。**审查（五轮）**：两轴各一独立只读子代理（均无 P0/P1；B 轴判 R4 主张 **holds**）+ **B 轴复跑**（P1 无点主机 userinfo / P2 密钥形词表未作用于 URL 与 detail / P2 throwing `cancel_reason_supplier` 复现修前症状 / P3 句柄分叉 docstring 误述；前三条修、**抛错 supplier 转登记** = 动公开参数的失败语义需单独授权）+ **三轮窄复验**（A 轴 P2 规则与注释都不实 + 5×P3；B 轴 P1 六处实测漏法 + 5×P2 钉子不足 + 4×P3，全部处置于 `c7acb8e`）+ **四轮证伪复验**（NEEDS-FIX，1×P2 + 4×P3：**`?` 隔离行是死钉子**——带 `https://` 被 authority 层先吃掉，删掉尾部集合里的 `?` 后 62 条全绿；处置于 `eadeb20`）+ **五轮闭合复核**（判四轮三条**真修上了**、`src/` 零改动、regex 一字未动；另出两条**新注释**的 finding（例子不成立、闭集括号写宽）⇒ 后置处置于 `3e77c1d`，注释里每条可测断言由 `claim_check` 逐条探针复核 **19 条 FAILS = none**；同时把四轮的 `39→42` 一条判为**口径误读** ⇒ 定稿两个计数口径都写）。**门禁（冻结树 `1821a7be…` = `c7acb8e` 的树；最终树 `3e77c1d` = tree `03c3f041…`）**：三轮树全量 **3044 passed / 2 skipped / 42 deselected / 9 warnings in 314.07s / exit 0**（= 二轮 3039 + 三轮 5 个新测试函数）、最终树同数 **3044 passed … in 301.86s / exit 0**（四、五轮只改注释与一行测试数据）、Phase 16 gate **12 passed**、`ruff` clean、`git diff --check` exit 0；**两条读数纪律**：`PYTHONPATH` 必须 Windows 形式且先断言 import 落点在 clone 内（本轮踩过 `/tmp/...` 静默回落主工作树），且冻结树必须 **git 支撑**（`git archive` 树会因 `test_smoke.py::test_regression_metadata_fields_are_honest` 的 `assert metadata["git_commit"]` **假红 1 条**——实测 archive 树 1 failed / 3043 passed vs clone 树 3044 / 0）。**父票 `#247`**：AC1–AC5 逐条取证（golden 243 / 配对·悬空 11（两文件全量；`-k` 只选中 3 条）/ 收口与释放 38 / 变异 M5 5 红与 M6 1 红；**上一版台账的 17 / 27 / 4 红 / 6 红 三处读数不实，本次按可复跑命令重测更正**，M6 的钉子实际在 `tests/web/test_runmanager.py`）⇒ **已按 §14.12 关单**（`gh issue view 247` 复核 = CLOSED；AC5 三个读数关单前又在最终树 `3e77c1d` 上整轮复采，逐一相同）。**集成与关单（已完成）**：台账笔 `dbe0d9a`（覆盖闸门 exit 0）⇒ `push origin main` 快进 `4503d37..dbe0d9a` + 收尾两支 `ffd0857`/`b17975e`（docs/台账 only）⇒ 三个 clone 对齐 **`b17975e`**（`HEAD^{tree}` = `452a6fe…`）⇒ `#247` CLOSED；**`#248` 保持 OPEN**（AC2 部分满足 + R1/R2 待裁决）。**并行线（非本批）**：另一会话在同一 clone 干 `#282`，本批全程路径限定提交（`git commit --only`）。明细见 `docs/phase_status/2026-09.md` L660。
- 2026-09-22（B-33 `#284` + B-34 `#285`，环境红与 `#265` 残余一起收口）：**B-33** 给依赖 `.env` 主模型链的 **18 条**真实调用挂上**环境守卫**（`tests/live_model_guard.py` 三态探测 + 四态裁决：任一端点可用 ⇒ 放行；全部**已分类**不可用 ⇒ 响亮 `pytest.skip`（逐端点列出理由）；**任一未分类失败 ⇒ 放行（fail-closed）**；配置不全（`ConfigError`）⇒ 放行，交给用例既有 `skipif`），判据 = "配置链里有没有任一端点能完成一次最小调用"（`ping` / `max_tokens=1`）。整仓 `-m integration` 由 **17 failed / 17 passed / 6 skipped** 变为 **`failed` 恒 0**——主树（真实 `.env`，守卫路径）**16 passed / 24 skipped**（18 条守卫 skip + 6 条既有 skip）；`%TEMP%` 冻结树副本（**无 `.env`**）**12 passed / 28 skipped**，那一轮的 28 条 skip **不是**守卫产生的（守卫按设计放行 `ConfigError`），而是用例自己的 `skipif`（`-rs` 实测）；**冻结树副本 + 合成假 key 的 `.env`**（零真实凭据，`MODEL_BASE_URL=https://127.0.0.1:9/v1`）**12 passed / 28 skipped / 0 failed**，skip 理由带 `[live-model guard]` 前缀 + 逐端点归因 ⇒ **守卫路径可在无凭据环境里被任何人复现**（passed 计数 16 vs 12 的差来自 `.env` 里有没有可选键 `FALLBACK_MODEL_*` / `TAVILY_API_KEY` / Milvus）。**18 = 17 红 + 1 条假绿**（`test_phase14_gate.py::TestGate4SessionsTreeReal` 的 fork 树渲染用例：也走真实 runtime 打真实模型，但断言面只读 `sessions --tree` 输出 ⇒ 模型失败它照样绿）。两轴审查的 2×P1 都是判据真缺陷：文本层未与本侧异常族划界（重构漏改的 `TypeError` 会被 skip 掉 = **吞代码回归**）、`MODEL_API_KEY=""` 只遮蔽守卫没遮蔽用例（两条路径读不同配置）；处置含 `settings_env_sealed()` 统一配置边界 + `SSLCertVerificationError` 先于 `ValueError` 家族判定（CPython 多继承）。**B-34** 收口 B-32 残余：R1（关键字集合回 #264 前口径，哨兵 `_Unset`/`_UNSET`）、R2+R3（**收口即清口**，取消臂与异常臂两条出口同时收口；公开参数 `keep_handle` 删除 = ④）、⑦（异常臂用例进仓库，`RejectingStore` 注入存储故障）、⑧（端到端断言面补到句柄身份 + 关键字集合）、⑤ 复核（`grep` 零命中）；**新逮到 R4**（`interrupt_streams()` 抛错 ⇒ 收口段整个不执行 ⇒ 该路径 0 次收集；既有形状，登记不阻断）。**审查 / 红证**：两批各两轴独立审查 + 修后窄复验（**均判"通过"**）；窄复验非阻断 P3 处置于 `8213164`（脱敏断言由空转改为有牙、外层预算断言、三处措辞 + 408 取舍登记）。定向变异：脱敏那条与预算那条"**恰好各一条转红**"；臂层变异 **2 条转红**（此前 0 条）；`-m integration` 缺配置路径在无 `.env` 副本里 **19 skipped / 0 error**（反向变异 ⇒ 15 error）。**门禁（`%TEMP%` 副本的冻结树，tree `1261df27…`；主工作树与 `HEAD` 不一致——并行会话推进了 `HEAD`、其工作树/index 停在 `3b4e93c` 承接基线，主树 `src`/`tests` 读数不代表本批冻结树）**：全量 **3016 passed / 2 skipped / 42 deselected / 9 warnings in 347.83s / exit 0**、Phase 16 gate **12 passed**、`ruff` clean、`git diff --check` exit 0；**覆盖闸门 exit 0**（台账 84 行、区间 `09ca47a1..HEAD`、387 提交 / 已审查 274 / 待判定 113）；**已集成**：`push origin main` 快进 `56d561b..bafbd12`（`origin/main` = `HEAD`，0/0）+ `#284` / `#285` 关单 + 随后 **4 笔 docs-only**（`f838a92` / `f28fcc5` / `4cd66ef` / `a080912`；`src`/`tests` 零改动）推到 **`a080912`** + 三方 clone 对齐 **`a080912`**。**并行线（非本批）**：另一会话在同一 clone 干 `#282`，本批全程路径限定提交（`git commit --only`），未触碰其 17 个陈旧路径。明细见 `docs/phase_status/2026-09.md` L628（B-33）/ L650（B-34）。
- 2026-09-21（B-32，`#265` telemetry 切片——T11 第二切片）：把一次 run 的**观测可变状态**（`tracer` + 在途 `ctx_span` / `generation`）收进单点 owner `_Telemetry`，**行为零变化**由两条判据锁定——#263 的 golden（`test_event_sequence_golden.py`）逐字未改且全绿 + 端口调用 **16 例差分**在 `09bae51` ↔ `7aef01b` 之间**结构签名逐项相同**（两轴各自独立复现）。**两轮两轴独立审查**：第 1 轮 A 轴 P0/P1/P2 = 0、P3×2；B 轴 P0/P1 = 0、**P2×2**、P3×2——**B 轴那条 P2 是阻断级、也是唯一的等价性破坏**：ctx 超限臂在 #264 之前**不清**在途句柄，「超限 + 消费方在终态帧上断连」时取消臂对同一 span 二次收口（旧形状端口调用 **6** 次，重构顺手清口后 **5** 次）⇒ `7aef01b` 以 `_Telemetry.context_build_completed(keep_handle=True)` **逐字复刻旧形状**（改行为需另开票 + 用户批准，§9.1.1），并把缺陷与两条出口登记为残余；第 2 轮 **B 轴判"通过"**（P0/P1/P2 = 0，另出 P3：第二条收集出口是**异常臂**、非只有取消臂）、**A 轴判"不通过"**（**代码面零返工**，三条全在 docstring / 记账面：类 docstring 自相矛盾、差异①的定性词"键集"与事实相反、缺一条真实断连的端到端用例）⇒ 三条由 `cef3bcd` 落地（含新增端到端用例；机械证据：AST 去 docstring 后两文件与 `2fb974f` **同构**）。**修后窄复验两轴**：B 轴判"通过"（自建两级探针 30 用例逐项相同、分辨力对照 7 处实质差异、R2/R3 各自复现并否证"第三条出口"）、A 轴判 A-1…A-4 全部闭合（另出 1×P1 台账在途 + 2×P3：R1 机制括注 → `7636547`、取证脚本未点名 → tracker R2 改写），两条新残余 ⑦（R3 无仓库内用例）⑧（端到端断言面只有方法名序列）。**残余登记在 tracker B-32 段**：R1（调用关键字集合差异，**端口输出无差异**）、R2/R3（超限臂二次收口的两条出口：取消臂 / 异常臂——修它的票必须同时覆盖），修不修由单独一张票决定。**父票 `#247` 保持 OPEN**（本票只关自己；三张子票 #263/#264/#265 均已交付，父票是否可关需先核其整体验收面）。**闭合复核（两个独立只读子代理）**：N-3 / N-4 闭合、N-2 具备兑现条件（彩排台账 exit 0）、4×P3 全在记账面（①②处置于 `90fc169`、③④处置于台账行本身）。**集成 / 关单**：`origin/main` 已被前端线推到 `3b4e93c` ⇒ 先回后正合并为 `d165740`，2 个冲突全在记账面（索引按合并后归档实测重算 295 条 / 621 行、台账追行取并集 172 行），合并的独立审查 6 项全 OK；冻结树门禁全量 **2915 passed / 13 skipped**（Docker 不在场）+ Phase 16 gate **11 passed / 1 skipped**、`ruff` clean、`git diff --check` exit 0；已 `push origin main`（`origin/main` = `HEAD`）并关单 `#265`。明细见 `docs/phase_status/2026-09.md` L609。
- 2026-09-21（B-31，既有红修复 + demo 同缺陷类残留 + 已知 flake 转生效）：`tests/integration/_phase16_helpers.py:245` 的 `_auto_approve` 同步 → `async def`（`e036681`，amend 前 `fd9882c`、**树未变**）、`demo/live_agent.py` 两处审批回调同缺陷类补 async（`1383e52`）、B-29 遗留的已知 flake 表翻**生效**。**用户批准**：① 修既有红（「Phase 16 Gate 1/12 红，根因即该行」）；② Docker 已启动，把 `test_timeout_stops_late_workspace_mutation` 补跑 3 次以转生效；demo 一项**不在点名范围**，按「我允许你执行新 tickets，但是要按照 v3 的方式开发」作为新票执行并走同一套审查与门禁。**红证（副本复算）**：把 `:245` 改回同步 ⇒ gate `1 failed, 11 passed`（断在 `:379`），修后 `12 passed`。**两轴审查（锚 `fd9882c`）**：A 轴 `P0/P2 = 0`、P1×2、P3×3（P1① = 台账缺行 ⇒ 覆盖闸门 exit 1，由本批台账行兑现；P1② = demo 残留）；B 轴 `P0/P1 = 0`、P2×1、P3×3，机制溯源（TypeError → 收口为**持久** `run/failed(reason=TypeError)` + INFO 诊断）**证伪**原提交信息「静默夭折」措辞 ⇒ 未 push 前 `git commit --amend` **只改信息**；**变异 R3（保持 async 但 `approved=False`）红在 `:390`** ⇒ 判据面对"审批放行"敏感、非空转。**门禁**：冻结树 `1383e52` 全量 **2913 passed / 2 skipped / 42 deselected / 546.21s / exit 0**（Docker 在场 ⇒ 上一批 11 条 docker 门控由 skip 转真跑；冻结前一手复跑 `fd9882c` 树采集数同、墙钟 606.00s）、`ruff` clean、`git diff --check` 无输出。**flake**：独立子代理 3 次（实为 5 次）全部 `1 passed`、并发探针实捕容器 `Up → Exited(137)` ⇒ 翻**生效**（保留项：历史失败未复现，属**潜伏**）。**集成**：三次快进 `push origin main`（① 代码面 `5003377..bb594e6` = 6 笔：判据① 4 行全 `M` docs-only ⇒ 全量读数传递、`ruff` clean；② 收尾 6 笔纯 docs/台账），覆盖闸门首跑 334/238/96、② 段复跑 336/238/98、收尾修订后再复跑均 **exit 0**，`origin/main` = `HEAD`、ahead/behind 0/0、无关单；**§14.9 通知**：两 clone 分别 **8/173** / **8/238** behind（前端 8 笔重叠同 4 个 docs 文件 ⇒ 预期冲突），开工前先自检。明细见 `docs/phase_status/2026-09.md` L576。
- 2026-09-21（B-30，文档治理清理）：`AGENTS.md` **907 → 822 行**（`523efc1` → `35268a3`；章节号与标题逐字未变；其后记录追加与 §13.4 逐字还原使行数回到 836（`93de00e`）/ 841（`20ee173`））、`docs/PHASE_STATUS.md` **132 行 / 63.5 KB → 85 行 / 23.1 KB**（同两节点，按 `git cat-file -s` 对象字节；"瘦身"读数即取这两节点，其后每次追加记录都会再变）、75 份一次性资料 `git mv` 进 `docs/archive/`（顶层 .md 147 → 73，不留 stub、映射在两目录 README）、新建 **`docs/README.md` 作为 `docs/` 唯一导航入口**；两轴独立审查首轮各 1 个阻断 findings（按日索引失真 / 历史 `git show <rev>:<path>` 被误改）修复后 P0/P1/P2/P3 = 0；**接手 delta（`93de00e`）第二轮两轴审查 P0 = 0 / P1×1（台账未覆盖该 tip ⇒ 覆盖闸门 exit 1 而记录写 exit 0）/ P2×3 / P3×6（去重后），修后重审 P0/P1 = 0、余 4 条一并处置**；闭合复验（两轴，锚 `d5e1a68`）上轮 findings 逐条闭合、另出 3 条（2 条已在 `b7108c7`；"`^{commit}` 是重定向符"的过度声明 1 条按实测改写）**；门禁：后端全量 2902 passed / 13 skipped（其中 11 条为 Docker daemon 不在场，reason 清单实测）、前端 tsc 0 错 / vitest 1040 通过 / oxlint 0 error / build 成功 / e2e 436 passed；集成：按 §13.2(b) 在施工 clone 的 `main` 上快进 `push origin main`（`523efc1..8602676` = 13 笔；覆盖闸门 exit 0、后端全量一手复跑 **2902 passed, 13 skipped, 42 deselected, 13 warnings in 398.97s (0:06:38)**、`ruff` clean、`git diff --check` 无输出、`origin/main` = `HEAD`、ahead/behind = 0/0）。。明细见 `docs/phase_status/2026-09.md` L566。
- 2026-09-21（B-29，V3.1-lite 提速增补）：协议 §8、两轮独立审查、已知 flake 与待裁决明细见 `docs/phase_status/2026-09.md` L565；当前流程权威仍为 `docs/SDD_WORKFLOW_PROTOCOL.md`。

### 归档文件

| 文件 | 覆盖日期 | 条目数 | 说明 |
| --- | --- | --- | --- |
| `docs/phase_status/2026-09.md` | 2026-09-03 .. 2026-09-26 | 320 | 历史明细；2026-09-17 初次迁移的正文逐字保留，后续批次按日期追加。条目数 = `grep -c "^- 2026-"` 在**合并后**的树上实测 = **320**（截至 9/23 为 311，9/24 增 3 条，9/25 增 4 条：B-37 收口、`#299` 实施与 AC10 收口、`#301`，9/26 增 2 条：服务端 Gate-0 + `main` 分支保护（PR #321）+ 覆盖闸门「空 `--cc` 合并提交」机械归属 + 陈旧文档全量订正；B-47 覆盖闸门第四条判据「零新增内容」）。**2026-09-25 的 B 链 T2/T3/T4 是 `## ` 小节**（不是 `- 2026-` 条目，故不计入条目数；T1 `#306` 的小节 L813-L897 随本次 T1 集成进入，同样不计入条目数（其**集成落账**小节 L1443-L1509 同属不计））。**2026-09-26 的 B 链 T5 `#313` 同样是 `## ` 小节**（`2026-09.md` L1283-L1441；**索引订正**：原记 L1281-L1439 —— `7bb6c8e` 在 T1 小节内净增 2 行、其后所有行号 +2 而索引未同步，本笔按实测重算），同样不计入条目数；T1 `#306` 的集成落账小节 L1443-L1509 同属；**T6 `#314` 同样是 `## ` 小节**（`2026-09.md` L1511-L1618），同样不计入条目数；其**集成落账**小节 L1620-L1653 同属，**同样**追加在 EOF（既有行号零位移）。**T7 `#315` 同样是 `## ` 小节**（`2026-09.md` L1658-L1881，块内末三笔提交落在 09-27 凌晨、按票据起始日归 09-26），同样不计入条目数；其集成落账小节 L1885-L1915 **同样**追加在 EOF（既有行号零位移）。更新与读取纪律、逐日行号见本文件「按日定位」表。 |

### 按日定位（归档内行号，日期降序）

| 日期 | 条目 | 位置 |
| --- | --- | --- |
| 2026-09-26 | 2（服务端 Gate-0 + `main` 分支保护（PR #321）+ 覆盖闸门「空 `--cc` 合并提交」处置（台账 209 行）+ 陈旧文档全量订正 / B-47 覆盖闸门第四条判据「零新增内容」）+ 6 个 `## ` 小节（B 链 T5 `#313` / T1 `#306` 集成落账 / T6 `#314` / T6 `#314` 集成落账 / T7 `#315` / T7 `#315` 集成落账，**小节不计入条目数**） | `2026-09.md` L1259-L1266（1 条顶层 + 7 条子项）/ L1268-L1279（1 条顶层 + 11 条子项；**子项数口径 = `^  - ` 顶层计数**）；小节：L1283-L1441（T5，起于 `## 2026-09-26 · T5（`；**索引订正**：原记 L1281-L1439）/ L1443-L1509（T1 `#306` 集成落账，起于 `## 2026-09-26 · T1（`）/ L1511-L1618（T6 `#314`，起于 `## 2026-09-26 · T6（`）/ L1620-L1653（T6 `#314` 集成落账，起于 `## 2026-09-26 · T6（`）/ L1658-L1881（T7 `#315`，起于 `## 2026-09-26 · T7（`；**末三笔提交落在 09-27 凌晨，按票据起始日归 09-26**；其集成落账小节 L1885-L1915 追加在 EOF）。**行号口径（2026-09-26 起）**：小节 = `## ` 头行 .. 该小节**自己块内**的最后一条非空行；条目 = 顶层 `- 2026-` 行 .. 其最后一条 `  - ` 子项。**在归档中部插入行必须同笔重算本表**（`7bb6c8e` 在 T1 小节内净增 2 行即让本表 09-25 / 09-26 两行全体失真，成因见 T1 集成落账小节）。 |
| 2026-09-25 | 4 条 `- 2026-` 条目（B-37 GitHub 收口 / MEM-V2-3 `#299` 实施 / MEM-V2-5 `#301` / `#299` AC10 收口）+ 4 个 `## ` 小节（B 链 T1 `#306` / T2 `#307` / T3 `#308` / T4 `#312`，**小节不计入条目数**） | `2026-09.md` L811（B-37）/ L1256（`#299` 实施）/ L1257（`#301`）/ L1258（`#299` AC10）；小节：L813-L897（T1；集成落账另见 09-26 行）/ L899-L982（T2）/ L984-L1105（T3）/ L1107-L1255（T4） |
| 2026-09-24 | 3（MEM-V2-2 `#298` / B-37 最终 Gate / B-37 验证补记） | `2026-09.md` L808-L810 |
| 2026-09-23 | 5（B-44 `#294` / B-43 `#286` / Memory V2 PRD + Tickets + Issues / B-45 `#295` / 集成与全量验证） | `2026-09.md` L783-L807（B-44 = L783；B-43 = L784 起含 11 条子项；Memory V2 = L797；B-45 = L798 起含 8 条子项；集成与验证 = L807；**子项数口径 = `^  - ` 顶层计数**） |
| 2026-09-22 | 11（B-33 / B-34 / B-35 / B-36 / F18-A `#282` / F18-B `#283` / B-38 / B-39 `#248` / B-40 `#291` / B-41 `#292` / B-42 `#293`） | `2026-09.md` L628-L780（**子项数口径 = `^  - ` 顶层计数**（更深缩进的另计），2026-09-22 按归档**实测重算**：B-33 = L628 起含 12 条子项，B-34 = L650 起含 8 条子项，B-35 = L660 起含 7 条子项，B-36 = L669 起含 13 条子项，F18-A = L684 起含 12 条子项，F18-B = L698 起含 12 条子项，B-38 = L719 起含 8 条子项，B-39 = L729 起含 7 条子项，**B-40 = L737 起含 15 条子项，B-41 = L753 起含 14 条子项，B-42 = L768 起含 9 条子项**） |
| 2026-09-21 | 10（B-24 / B-25 / B-26 / B-27 / B-28 / B-29 / B-30 / B-31 / #274 B6 集成/关单 / B-32） | `2026-09.md` L512-627（B-29 = L567，B-30 = L568 起含 8 条子项，B-31 = L578 起含 13 条子项，**#274 B6 集成/关单 = L593 起含 8 条子项**，B-32 = L609 起含 17 条子项） |
| 2026-09-20 | 8 | `2026-09.md` L485-511 |
| 2026-09-19 | 20 | `2026-09.md` L465-484 |
| 2026-09-18 | 15 | `2026-09.md` L449-464 |
| 2026-09-17 | 35 | `2026-09.md` L413-448 |
| 2026-09-16 | 4 | `2026-09.md` L409-412 |
| 2026-09-15 | 11 | `2026-09.md` L388-408 |
| 2026-09-14 | 11 | `2026-09.md` L318-387 |
| 2026-09-13 | 14 | `2026-09.md` L290-317 |
| 2026-09-12 | 23 | `2026-09.md` L256-289 |
| 2026-09-11 | 25 | `2026-09.md` L221-255 |
| 2026-09-10 | 6 | `2026-09.md` L210-220 |
| 2026-09-09 | 9 | `2026-09.md` L194-209 |
| 2026-09-08 | 14 | `2026-09.md` L166-193 |
| 2026-09-07 | 18 | `2026-09.md` L137-165 |
| 2026-09-06 | 33 | `2026-09.md` L79-136 |
| 2026-09-05 | 20 | `2026-09.md` L46-78 |
| 2026-09-04 | 15 | `2026-09.md` L18-45 |
| 2026-09-03 | 4 | `2026-09.md` L14-17 |
