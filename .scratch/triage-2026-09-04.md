# Triage 清单 — 前端质量排查遗留项（2026-09-04）

> 来源：/code-review 两轴（Standards + Spec 并行子代理）+ /diagnosing-bugs 排查 +
> 并行会话独立 review（251b1b2 / da8b2ec，与本清单交叉验证）。
> 状态标签沿用 triage 状态机：`ready-for-agent` / `ready-for-human` / `wontfix` / `needs-triage`。
> GitHub Issues 是单一事实源（AGENTS.md）；本文件是工作区暂存，确认后应搬运为 issue。

## ready-for-human（需要人拍板）

1. **Radix 冻结空转** — ADR-0005 冻结「Radix 解决可达性坑」，6 个依赖零 import。
   建议处置：不改存量（现控件手写 aria 已达标），立约定「新浮层一律 Radix」。
   若否决此约定，应记 ADR 说明为何放弃 Radix 依赖（或从 package.json 移除 6 个依赖）。
2. **手动主题 / onboarding / markdown 渲染器的规格追溯** — 三者来自用户直接指令
   （非 spec/ADR 要求），属"用户当前明确指令 > 冻结规格"（AGENTS.md §1.1）。
   建议：在 ADR-0005 追加一小节或新 ADR 记录，避免未来审查反复标为 scope creep。

## ready-for-agent（规格明确，可指派）

3. **断连一致性第 4 条的后端收尾** — spec 11 §4：断连取消 run_stream 后 durable log
   留下 run/started 无 run/failed 收尾（无 reconcile/终止事件）。测试已断言现状
   （恰好 2 条、无半写行），但「谁负责补终止事件」未定义——属 Phase 4
   Recovery / Operation Ledger 域。**归属后端会话**，前端不越界。
4. **工具 duration 时间戳精度** — E2E 显示所有工具 1ms（started_at/completed_at
   同毫秒）。前端如实显示差值；根因是后端事件时间戳精度。建议后端 tool/result
   落盘时带真实耗时字段（duration_ms），前端优先读它。
5. **架构候选 ①：projection.ts resolveStepId 收敛** — 见
   `%TEMP%\architecture-review-20260904.html` 候选①（Strong，~30 行，零行为变化）。

## wontfix（已核实不修，含理由）

6. **STREAM_ONLY_TYPES 前端无消费者** — 保留：它是 event.py 词汇的完整镜像
   （生成器天然输出），非前端造的抽象；首个消费者出现即用。
7. **浅色主题终端色块** — 已修（589643d：--terminal-* token + 强制浅字）。
8. **_last_result 并发竞态（AgentRuntime）** — pre-existing，属后端 run() 同步
   入口的已知债，本轮前端会话不碰（Scope Lock）。已在其 commit 历史被后端
   会话标记过，留给后端。
9. **断连测试时序耦合（sleep(0) 投递 disconnect）** — 依赖 sse-starlette 内部
   调度；改为等 body 帧会把脆弱性换成另一形状（依赖首帧行为）。现状契约
   断言稳定，保持并观察。

## needs-triage（信息不足，等用户）

10. **是否引入 Vitest** — 前端零测试框架，架构候选 ③（useSession 状态机）的
    测试收益被此阻塞。决策点：stack 冻结（ADR-0005）是否扩一项 devDependency。
